MOONLIGHT 0909 FAZ — gerçek iş planı (9 Eyl 2026, CTO; hakem turu 1 + insan eleştirmen turu sonrası)
Yasa: docs/0809-kosu.md A (9 Eyl kararları dahil). Gösterim sırası B (R1..R21) DEĞİŞMEZ; yalnız iş sırası değişir. Tek istisna: R2 reels'i T05b sonrası çekilir (ay burcu o zaman yorumda).
Boy: S ≤150, M ≤500, L >500 satır; alt görev hedefi ≤300 satır, ≤8 dosya. "3 örnek" = D tablosuna kopyalanan 3 yorumun ilk 2 cümlesi.
Kararlar: asset dosyalarına dokunulmaz; kod dosyaları silinmez, kullanımdan kalkar; DailyReadingService LLM çağrısı kalkar (şablon cümle); günlük KH hesapsıza da gelir, giriş = senkron + davet + web; DeviceCheck yalnız sayar.

=====================================================================
DAMLA'NIN GÖRDÜĞÜ (tek sayfa)
=====================================================================
Elle yapacakların (ajan ekran ekran tarif eder):
- K2a (faz 2 başı, yorum kalitesinden önce): `npx wrangler login` + `npx wrangler d1 create moonlight_db` → database_id. 10 dk.
- K2b (cüzdan/reklamdan önce, T12'den önce): AdMob hesabı + 2 rewarded ad unit (onay günler sürer, ERKEN başla); App Store Connect kh1/kh3/kh10 consumable.
- K4 (faz 4 başı): Apple Sign in capability + Services ID; Google OAuth client id; mail servisi (Resend) + alan adı DNS (yalnız e-posta girişi istenirse).
- Çizimler (faz 5): docs/damla_tasks_moonlight.md listesi.
Kararların: (1) mock kalıbı (T15 sonrası, R8 bunu bekler); (2) Haiku mu Sonnet mu (T05b'nin 6 örneği); (3) web okuma bu turda mı (T34); (4) minör kart metinleri kamu malı kaynaktan mı, ajan mı yazsın (T26b); (5) T26b/T27 için 5 rastgele kartı sen okursun. Verilmiş karar: isim zorunlu, isimsiz yorum yok, hesapsızda da (T02 kapı kesin).
Faz sonunda ekranda ne var: F1 isimli tarot + horary cevabı · F2 3/5/Kelt açılım, "anlamadım" ek kartı, gökyüzü + göstergelerle yorum, cüzdan (coin, süreli/süresiz KH, 15→1, 4+1), reklam → KH · F3 evren navigasyonu, -2 hafta/+1 ay gökyüzü çizgisi, gerçek ay gölgesi · F4 Apple giriş, profil (gündem, sık konular, geçmiş), iki cihaz aynı hesap, kart rehberi + "N kez geldi", web'de geçmiş · F5 yeni resimler · F6 kart→gezegen/burç katmanı, doğum haritası evrende · F7 güvenlik + hız + submit.

=====================================================================
0. PLANI BELİRLEYEN ÜÇ GERÇEK
=====================================================================
- D1 yaratılmamış (wrangler.toml:20 placeholder); env.DB yok → auth/me/profile 500. K2a bunu açar; yorum kalitesi (T03-T05) yalnız K2a'yı bekler, AdMob/ASC'yi (K2b) beklemez.
- /api/claude (worker.js:57,283-302) app-token'lı açık proxy; token ipa'da. Yorum + borç aynı uca taşınır (T05a, borç T09b). Model: worker.js:288 haiku → Sonnet (T05a).
- Worker deploy: ajan `npx wrangler deploy` ile yapar; Damla K2a'da bir kez login verir.

=====================================================================
1. R → İŞ (gösterim sırası)
=====================================================================
| R | Alt görevler | Boy | Bağımlılık | Risk | Kanıt |
|---|---|---|---|---|---|
| R1 Tarot ismiyle | T02 isim kapısı (Damla kararı: isim zorunlu, hesapsızda da) | S | — | Hesapsız ilk falda huni daralır, kabul edildi | sim: isimsiz → kapı; 3 örnek yorumda isim |
| R2 Gökyüzü yoruma girdi | İş 0; reels T05b SONRASI çekilir (ay burcu + iç gezegen retro o zaman prompt'ta) | — | T05b | T05b'den önce ekranda ay burcu görünür ama yorumda yoktur | prompt'ta {{sky}} satırı; Home olay listesi |
| R3 Horary | İş 0 (derinleşme T18a/b, R4 bloğunda) | — | — | HoraryView:175 `try?` chart'sız devam; T18a'da "chart yoksa istek yok" | sim: soru → cevap |
| R4 Kombinasyon | T03, T04, T05a, T05b, T18a, T18b | L (6) | K2a, T01 | ClaudeService.tarotReading çağrılmaz olur; 3/5 kartta pozisyon adı prompt'a girmez | XCTest; node test; 3 örnek: ilk cümle isim + cevap, sayımlar hikâyeye yedirilmiş, kart kart paragraf yok; Haiku vs Sonnet 6 örnek |
| R5 Anlamadım + açılımlar | T06, T07 | M+M | T05b | TarotView:183/425 "3 kart" sınırı açılıma göre; Kelt max_tokens T05a'da açılıma göre | sim 3/5/10; ek kart yorumu "ilk açılımda X yoktu" |
| R6 Coin + KH | T01, T08, T09a, T09b, T10a, T10b, T11, K2b, T12, T13, T14 | L (11) | K2a; K2b yalnız T12-T13 önünde | CreditManager'ı 6 view okuyor → T10a ince adaptör, build hiç kırılmaz; DEBUG 500 (CreditManager:68-73) biter | XCTest+node aynı vektörler; sim: 14 coin'de 15→1 pasif; 4. fal (horary dahil) +1 süresiz KH |
| R7 Mock | T15 | S | — | Damla karar vermezse R8 başlamaz | Damla kalıp seçer |
| R8 Evren navigasyonu | T19a, T19b | L (2) | R7 kararı | CreditBadge/.padding(.top,54) yerleşimleri kayar | sim: 4 fal yeri (2 "yakında") |
| R9 Anasayfa gökyüzü | T16, T17, T20, T21 | L (4) | T16-17 T18a'ya da lazım | AstrologyService/MoonService USNO çağrılmaz olur, silinmez; widget ayrı formül (MoonlightWidget:17-50) → SkyEngine | XCTest: 2026-03-03 ve 2026-08-28 ay, 2026-08-12 güneş tutulması; sim: -2 hafta/+1 ay; ay gölgesi aydınlanmaya göre |
| R10 Horary haritası ("soruyu sorduğun saniyenin haritası") | T18b (çekim T18b sonrası; iş R4 bloğunda) | — | T17, T18a | Gösterge/boş seyir cümlesi yorumda yoksa reels boş | sim: harita ekranı + yorumda gösterge cümlesi; chart yoksa istek yok |
| R11 Apple giriş + profil + senkron + web ("telefon değiştirdim, falım orada") | K4, T22, T23a, T23b, T24, T25, T34 | M+L+M+M+M | T09a, T03 | Apple ismi yalnız ilk yetkide, anında POST; son yazan kazanır yalnız favori/silme | iki sim aynı hesap; /api/me isimli; cüzdan birleşme kuralına göre; tarayıcıda geçmiş |
| R12 Kart rehberi | T26a, T26b1-4, T27 | M+4S+M | T03 | CardDetailView:8-57 replace zinciri kalkar; TarotCard.name İngilizce anahtar | 78 kartı gezen rehber; Damla 5 rastgele kart okur; "N kez geldi" ReadingStore ile eşit |
| R13-15 Resimler | T28a/b/c (yeni dosyalar eklenir, mevcutlara dokunulmaz) | S×3 | Damla çizer | Ad değişirse Image("…") kırılır; adlar damla_tasks ile birebir | build; ekran görüntüsü |
| R16 Astrolojik karşılık | T33 (kaynak Damla) | M | T04, T26a | Prompt uzar | 3 örnek: gezegen/burç cümlesi; rehberde alan dolu |
| R17 | Faz 5 sonunda hakem | ? | — | — | — |
| R18 Doğum haritası | T29 (BirthChartView + NatalChartService kodda var, SettingsView:101) | S | R8 | BugunView ölü, dokunulmaz | sim: evrenden haritaya |
| R19 Güvenlik | T30a (DeviceCheck .p8 + App Attest + IAP JWS + CORS + limit), T30b metinler | M+M | hepsi | privacy-policy.html R6'dan itibaren yanlış; R19 öncesi TestFlight yok | curl 401/403; grep secret |
| R20 Hız | T31 | S/M | R19 | — | önce/sonra sayılar |
| R21 Submit | T32 | S | R19-20 | isim çakışması (rakipler.md "Moonlight") | ASC "Waiting for Review" |

=====================================================================
2. ALT GÖREVLER (tek oturum, ≤300 satır, ≤8 dosya). Girdi = ajanın okuduğu dosyalar (satır); A + ilgili spec bölümü her pakete eklenir.
=====================================================================
| T | R | Ad | Girdi (satır) | Çıktı | Kanıt | Hakem kontrol |
|---|---|---|---|---|---|---|
| T02 | R1 | İsim kapısı (karar kesin) | TarotView(634), HoraryView(277), UserProfile(142), BirthChartView:300-448 → 1200 | NameGateView.swift; TarotView:432 revealCards / HoraryView:153 askQuestion önünde (kart seçilir, açmadan önce isim istenir); isim boşsa istek gitmez, hesapsızda da | sim: isimsiz → kapı; 3 örnek | A "İsmi soracağız"; Damla 9 Eyl "isim zorunlu, isimsiz yorum yok" |
| K2a | R4 | Kurulum: wrangler + D1 (Damla) | backend/README.md, wrangler.toml(20), schema.sql(21) → 60 | `wrangler login`; `d1 create` + database_id; `d1 execute --remote` | uçlar 500 vermiyor | A "elle kurulumlar her fazın ilk alt görevi" |
| T01 | R6 | XCTest hedefi | project.yml(70), Moonlight.xcodeproj (xcodegen ile üretiliyor, /opt/homebrew/bin/xcodegen kurulu), TarotCard(193), MoonPhase(57) → 320 | MoonlightTests target + scheme; SmokeTests.swift (78 kart, evre eşikleri). Önce `git status` ile .xcodeproj'un elle düzenlenmediği doğrulanır | `xcodebuild test` yeşil | A "XCTest (faz 2'de kurulur)" |
| T15 | R7 | Mock | mock/moonlight-mock.html(268), spec 7, ContentView(90) → 450 | Aynı HTML'de 3 kalıp (katmanlı sheet / gökyüzü haritası / yatay odalar), gerçek ekran metinleriyle (gri kutu değil); haritada 4 fal yeri = gökyüzünde 4 konak | Damla seçer | A "evren hissi, karar Damla" |
| T03 | R11/R12 | ReadingStore v2 | ReadingHistory(83), ReadingHistoryView(196), TarotView:573-613, HoraryView:153-204, SettingsView:505-520 → 420 | ReadingRecord +cards[id], spread, sky, topic?, updatedAt, dirty; Application Support/readings.json; UserDefaults'tan taşıma (eski kayıtlarda cards boş → "N kez" o kayıtları saymaz, kabul); maxRecords kalkar; şema T24 için şimdi XCTest'le sabitlenir | XCTest kaydet/oku/taşı | D "50 sınırı kalkar"; spec 6 |
| T04 | R4 | SpreadAnalysis | TarotCard(193), ClaudeService:36-86 → 250 | TarotCard +number, +rank, +element, +astro(boş); SpreadAnalysis.swift: majör/element/eksik takım/tekrar sayı/saray → kısa cümleler; testler | XCTest 8 vaka | spec 1 katmanlar; spec 1:27 "hikâyeye yedirilir" |
| T05a | R4 | Worker ucu + şablon | worker.js(332), wrangler.toml, Localization:1-30 → 400 | backend/prompts/tarot.txt ({{name}} {{question}} {{spread}} {{cards}} {{analysis}} {{sky}} {{agenda}} {{language}}; 3/5 pozisyonsuz, Kelt 10 pozisyon; kural: ilk cümle isim + cevap, sayım tek paragraf olmaz); backend/prompt.js; /api/reading/tarot; model Sonnet; max_tokens açılıma göre; backend/test/prompt.test.js | node test; curl ile 1 yorum | A "3/5 pozisyonsuz", "Sonnet" |
| T05b | R4 | İstemci + karşılaştırma | ClaudeService(280), TarotView:573-613, ReadingAPI → 500 | Services/ReadingAPI.swift; TarotView call-site; Element "%" prompt'tan çıkar; ClaudeService.tarotReading çağrılmaz | aynı 3 açılım Haiku vs Sonnet 6 örnek D'ye | A "faz 2'de karşılaştırma Damla'ya" |
| T06 | R5 | Açılım UI | TarotView(634) → 640 | 3/5/Kelt seçici; seçim sınırı; ilişki+kariyer (TarotView:206-207, ClaudeService:26-29) kalkar; 3/5'te pozisyon etiketi yok | sim | A "3 ve 5 kart, Kelt"; "yapma" ilişki/kariyer |
| T07 | R5 | Anlamadım | TarotView:444-498, ClaudeService:172-197, ReadingAPI, prompt.js → 400 | "Anlamadım" → ek soru + 1 ek kart; prompts/tarot_followup.txt (önceki kartlar+yorum+ek soru+ek kart+aynı gökyüzü; ek kart element sayımında); /api/reading/tarot-followup | node test; sim | A "ek soru + ek kart, 3 coin" |
| T08 | R6 | Wallet modeli + vektörler | CreditManager(280) → 280 | Models/Wallet.swift (saf); MoonlightTests/wallet-vectors.json (13, horary dahil); WalletTests; backend/wallet.js + test aynı vektörler | XCTest + node | A sayıları; "horary 4+1 sayar" |
| T09a | R6 | D1 v2 + anon | worker.js, schema.sql → 350 | schema: users(+name,+anon,+invite_code,+invited_by), wallets, kh_timed, readings, agenda, iap_transactions; /api/anon (anon_id + JWT); günlük KH hesapsıza; DeviceCheck yok (T30a) | node test; curl | A "Keychain anonim kimlik + sunucu" |
| T09b | R6 | Cüzdan uçları | worker.js, wallet.js → 450 | /api/wallet GET, /convert, /iap (T10b ile; JWS imzası T30a'ya kadar yalnız decode + transaction_id tekilliği — ARADA doğrulanmamış makbuz, risk kabul, TestFlight R19 sonrası), /daily-claim; /reading/* borç/iade/4+1 atomik | curl akışı | spec 4 "sunucu doğrusu" |
| T10a | R6 | İnce adaptör | CreditManager(280), CreditBadge(53), 5 view çağrı satırları → 500 | WalletManager.swift CreditManager'ı sarar; 6 view WalletManager'a bağlanır; davranış aynı | build; sim aynı | — |
| T10b | R6 | Sunucu cüzdanı | WalletManager, KeychainHelper(56), ReadingAPI → 400 | anon_id Keychain, JWT, fetch/cache, optimistic düşüm; StoreKit kh1/kh3/kh10 → /wallet/iap; Moonlight.storekit yerel yapılandırma (kh1/kh3/kh10, scheme'e bağlanır; ASC'yi beklemez), ASC gerçek ürünleri K2b'de, sandbox testi K2b sonrası; CreditManager çağrılmaz olur (dosya durur) | sim bakiye = sunucu | A "credits ve Plus kalkıyor" |
| T11 | R6 | Cüzdan ekranı | NoCreditView(278), CreditBadge, SettingsView:258-410, WelcomeView(101), NotificationManager:103-118 → 700 | WalletView.swift (coin, süreli KH + süre, süresiz, 4+1, 15→1 butonu, günlük KH, davet alanı yalnız hesaplı ve yalnız T14 yapıldıysa ("yakında" yok, yoksa satır yok), paketler); rozet; metinler | sim | A cüzdan paragrafı |
| K2b | R6 | Kurulum: AdMob + ASC (Damla) | — | AdMob hesabı + 2 rewarded ad unit (onay günler; K2a ile aynı gün başvur); ASC kh1/kh3/kh10 | ad unit id'leri; ASC ürünleri "Ready to Submit" | A "elle kurulum" |
| T12 | R6 | Fiyat çağrı yerleri | TarotView, HoraryView, WalletManager → 950 | 1/2/3 KH, ek kart 3 coin, Horary 1 KH, ek soru 3 coin; yetmezse istek gitmez; iade aynı kova; horary 4+1'e girer | sim | A fiyatları |
| T13 | R6 | AdMob | project.yml, Info.plist, WalletView, worker.js → 500 | GoogleMobileAds SPM (8 GB makinede ilk derleme uzun, tek oturum); UMP izin formu (EEA) + ATT kararı (şu an tracking=false, kişiselleştirilmemiş reklam); 2 rewarded; SSV imza doğrulama → cüzdan; SKAdNetworkItems; ilk ekran kaydı TEST ad unit ile | sim test ad; SSV logu | A "reklam 1 coin", "uzun → KH", "günde 3" |
| T14 | R6 | Davet kodu | worker.js, WalletView → 400 | invite_code (hesaplı), /invite/redeem, arkadaşın ilk yorumu → ikisine +5 (tek sefer), kendi kodu ret | node test; iki sim | A "davet, sunucu doğrular" |
| T16 | R9 | SkyEngine ay | project.yml, MoonService(150), MoonPhase(57), AstrologyService:37-82 → 350 | Packages/SkyEngine: CAstronomy (astronomy.c/.h, MIT) + Swift: illumination, phase, quarters(-14,+30), moonSign, ingresses | XCTest bilinen dördün/dolunay ±1 sa (USNO tablosu tarihleri) | A "ayın şekli evreye göre" |
| T17 | R9 | SkyEngine gezegenler | SkyEngine, HoraryChartService:44-61 → 400 | longitude+speed, ingress, retroStations, voidOfCourse, eclipses | XCTest: 2026-03-03, 08-28 ay; 08-12 güneş (NASA tablosu) | spec 3 |
| T18a | R4 | HoraryFacts (Swift) | HoraryChartService(329), SkyEngine, ClaudeService:90-145 → 600 | Yöneticiler, 1. ev yöneticisi, Ay, gösterge açıları uygulayan/ayrılan + orb + kalan derece, Ay'ın sonraki açısı, boş seyir; evler yalnız API /houses | XCTest 6 vaka = YAYINLANMIŞ horary örnekleri: Lilly Christian Astrology 1647 (Skyscript'teki örnek haritalar, tarih/saat/yer + kitabın gösterge sonucu) + Frawley Horary Textbook örnekleri; beklenen değer kaynaktan, ajan uydurmaz, kaynak bulunamayan vaka yazılmaz | spec 2 "hazır hesaplanmış"; "chart yoksa istek yok" |
| T18b | R4/R10 | Horary prompt Worker'a | worker.js, prompt.js, HoraryView(277), HoraryFacts → 700 | prompts/horary.txt + followup; /api/reading/horary: kategori→ev sınıflandırma (küçük çağrı, backend/houses.js) sonra ana yorum; ek soru chart görür; ClaudeService çağrılmaz olur | node test; 3 örnek | A "soruya göre cevap" |
| T19a | R8 | Evren kabuğu | ContentView(90), HomeView(316), Theme(180), MoonSceneView(177), seçilen mock → 900 | Seçilen kalıba göre kök view; 4 fal yeri; Settings/Wallet giriş | sim | A "evren hissi" |
| T19b | R8 | Geçişler | T19a, TarotView/HoraryView üst 60 satır → 700 | jest/animasyon; rozet/üst boşluk uyar | sim video | aynı |
| T20 | R9 | Gökyüzü çizgisi | AstrologyService(189), AstroEvent(63), HomeView(316), DailyReadingService(48), NotificationManager:120-150 → 750 | SkyEvents.swift (-14/+30); AstrologyService/USNO/sabit tutulma çağrılmaz olur; HomeView dikey çizgi; DailyReadingService LLM çağrısı kalkar → şablon cümle | sim; XCTest olay sayısı | A "-2 hafta ve +1 ay" |
| T21 | R9 | Ay çizimi + widget | MoonSceneView:167-176, HomeView:242-248, MoonlightWidget(170), NotificationManager:183-224 → 450 | Aydınlanmadan gölge maskesi (elips terminatör); widget + bildirim SkyEngine (sadece evre sınırında fark, düşük öncelik: bloğun sonunda) | sim; widget = uygulama | A "ayın şekli" |
| K4 | R11 | Kurulum faz 4 (Damla) | — | Apple Sign in capability + Services ID; Google client id; Resend + DNS yalnız T23b yapılacaksa | test mail; token doğrulanır | A "elle kurulum" |
| T22 | R11 | Apple ile giriş | worker.js:76-171, schema, WalletManager, SettingsView:1-110 → 700 | AuthManager.swift (fullName ilk yetkide anında POST; isim zaten varsa tekrar sorulmaz), SignInView.swift; anon→hesap BİRLEŞME KURALI: coins toplanır, kh_perm toplanır, kh_timed birleşir (süreler korunur), spreads_since_gift büyük olan, daily_kh_claimed_on ikisinden en yenisi (aynı gün ikinci günlük KH yok), readings taşınır, hesap kimliği kazanır, anon satırı kapanır | sim: giriş → /api/me; iki bakiye toplamı | A "Apple ile giriş"; spec 5 |
| T23a | R11 | Google | worker.js:205-216, AuthManager → 400 | GoogleSignIn SPM; JWKS yerel doğrulama; e-posta UNIQUE birleştirme | sim | A "Apple, Google, e-posta" |
| T23b | R11 | E-posta + şifre (kullanıcı yokken erken; Damla "şimdi" derse) | worker.js, schema, SignInView → 500 | PBKDF2 ≥600k; verify/reset; Resend | node test; sim | aynı |
| T24 | R11 | Senkron | ReadingStore, worker.js, schema → 600 | /api/readings push/pull, /api/agenda; updated_at sunucu saati; son yazan kazanır (favori/silme); kuyruk | iki sim | A "iki cihazda aynı hesap" |
| T25 | R11 | Profil + konu | SettingsView(520), BirthChartView:300-448, reading uçları → 900 | ProfileView.swift: isim, burç, gündem (son 5 soru), sık konular (readings.topic: yorum çağrısının sonuna "KONU: x" satırı, ek çağrı yok), geçmiş, cüzdan; {{agenda}} prompt'a | sim; 3 örnek | A "sık konular LLM çıkarır", "gündem birikir" |
| T34 | R11 | Web okuma (Damla kararı: bu turda mı) | app/page.js, app/layout.js, worker.js CORS → 300 | Repodaki Next.js: /gecmis sayfası; Sign in with Apple JS → aynı JWT; /api/readings GET + /api/wallet GET salt okuma; CORS web alanına | tarayıcıda geçmiş + bakiye | A "aynı hesap webden de okunabilir"; spec 5:125-126 |
| T26a | R12 | Kart verisi: 22 majör + yükleyici + rehber ekranı | TarotCard(193), CardDetailView:59-64,206-244 → 260 | Resources/cards.json şeması; 22 majör metin; TarotCard JSON'dan; Views/CardGuideView.swift: 78 kartı gezen liste (majör / 4 takım) | XCTest 22 kayıt; sim rehber | A "tarot rehberi gibi kullanacak" |
| T26b1-4 | R12 | Kart verisi: Asalar / Kupalar / Kılıçlar / Tılsımlar (14'er kart) | cards.json, TarotCard:129-188 → 300 | Damla kararı: kamu malı kaynaktan (Waite 1910 Pictorial Key, çeviri) ya da ajan yazar; takım başına 14 kart × 2 dil | XCTest 78 kayıt boş alan yok; Damla 5 rastgele kart okur | spec 6 "56 minör özgün metin" |
| T27 | R12 | Kart sayfası + sayım | CardDetailView(245), ReadingStore, TarotView:243-288 → 600 | replace zinciri kalkar; arketip/sayı/element; "N kez geldi, son: tarih, en sık: açılım" | sim | spec 6 |
| T28a/b/c | R13-15 | Asset takası | Assets adları, damla_tasks(177), MoonSceneView, HomeView, TarotView kart hücresi → 400 | a: ay + gökyüzü; b: 78 kart + back/frame; c: ikonlar + kedi + app icon 1024; yeni dosyalar eklenir, mevcutlara dokunulmaz; .interpolation(.none) kalkar | build; ekran görüntüsü | damla_tasks |
| T33 | R16 | Astrolojik karşılık katmanı (kaynak Damla) | cards.json, prompts/tarot.txt, SpreadAnalysis, CardDetailView → 500 | cards.json astro alanı; eşleme tablosunu Damla verir ya da onaylar (ajan kaynak önerir, seçmez); prompt'a "kart → gezegen/burç" satırı + gökyüzüyle çakışma; rehberde bölüm | 3 örnek; XCTest 78 dolu | A "faz 6, App Store öncesi" |
| T29 | R18 | Harita evrene | BirthChartView(448), T19a → 700 | Doğum haritası evrenden ulaşılır | sim | A "BELKİ doğum haritası" |
| T30a | R19 | Worker sertleştirme | worker.js, wrangler.toml → 500 | DeviceCheck .p8 (Damla verir) + bit0 sayacı; App Attest; IAP JWS Apple kök zinciri; CORS; kullanıcı başına limit; /api/claude ve /planets,/aspects kapanır; secret taraması | curl 401/403; grep | CLAUDE.md §6 |
| T30b | R19 | Metinler | privacy-policy.html, terms.html, PrivacyInfo, SettingsView:411-500 → 700 | KVKK aydınlatma + açık rıza; yurt dışı aktarım; sunucudan silme ucu + buton | Damla okur; sim silme → 404 | spec 5 |
| T31 | R20 | Hız | tüm servisler | açılış, /houses, yorum gecikmesi, prompt token, ipa boyu; en büyük 2 kalem | önce/sonra | A "ölçüm, sonra" |
| T32 | R21 | Submit | PROJECT.md, ASC | 1024 ikon, ekran görüntüleri, credits5/10/15+plus satıştan kaldırma, review notu (kombinasyon + efemeris + horary = 4.3(b) farkı) | ASC durumu | — |
Toplam: 50 alt görev (K2a, K2b, K4; T01-T34; T05/T09/T10/T18/T19/T23/T30 ikiye, T26b dörde, T28 üçe).

=====================================================================
3. ÇAKIŞMALAR ve KARARLAR
=====================================================================
| Çakışma | Karar | Gerekçe |
|---|---|---|
| R4 yorum kalitesi AdMob/ASC'yi bekliyordu | K2 ikiye: K2a (login + D1) T03'ten önce; K2b (AdMob + ASC) yalnız T12-T13 önünde; DeviceCheck .p8 T30a'ya | Yorum, reklam hesabına bağlı değil |
| R2 reels ekranda ay burcu, yorumda yok | R2 T05b sonrası çekilir (reels dosyasına aynı karar) | Ekranda yorumun kullanmadığı şey gösterilmez |
| R6 cüzdan cihazda, R10-11 sunucuda | Cüzdan ilk günden sunucuda (/api/anon, Keychain anon_id); R11'de T22 birleşme kuralı | A "Keychain anonim kimlik + sunucu" |
| Tarot prompt cihaz/sunucu | Sunucuda (T05a) | Borç + yorum atomik; token ipa'da açık; web aynı ucu kullanır |
| Horary derinleşmesi B'de reels'i yoktu | R10 = horary haritası (karar); iş R4 bloğunda (T18a/b), çekim T18b sonrası; Apple giriş R11'e | Wrapper olmayan çekirdek: harita + efemeris + hafıza |
| Gezegen hızı API'de var mı DOĞRULANMADI | SkyEngine (T16-17) T18a'dan önce | API'ye yalnız /houses kalır |
| Kart id'li geçmiş R12'de lazım | T03 R4 bloğunun başına | "N kez" boş başlamasın |
| CreditManager'ı 6 view okuyor | T10a adaptör → T10b → T11 → T12 | Her adım build yeşil |
| Erken/gereksiz (eleştirmen) | T23b e-posta, T14 davet, T21 widget bağı: bloğun sonuna, Damla "şimdi" demezse faz kapanışını beklemez; R6 çekimi T13 sonrası, T14'e bağlı değil | Kullanıcı yokken üç servis kurulmaz |
| Web okuma A'da var, plan yoktu | T34 eklendi, Damla "bu turda yok" derse A'dan düşer | Söz verildiyse planlanır |
Önce altyapı: K2a → T03-T05; T01 → R6; T03 → R11/R12; T05a → R4; T09a → R6/R11; T16-17 → T18a.

=====================================================================
4. ALTYAPI (3'er satır)
=====================================================================
(a) Efemeris: astronomy-engine tek C dosyası (MIT) `Packages/SkyEngine/Sources/CAstronomy/`; SwiftPM C hedefi, köprü yazılmaz; üstüne Swift `SkyEngine`. project.yml `packages:` + iki hedefte dependency. SwiftAA/Swiss Ephemeris (AGPL) yok. SkyEngine + HoraryFacts ayrı paket olarak açık kaynağa çıkabilir (SF'ye taşınabilecek tek parça).
(b) Veri: Application Support altında Codable JSON (readings/agenda/wallet-cache) tek `ReadingStore` actor'ü; SwiftData/GRDB yok. D1: users, wallets, kh_timed, readings, agenda, iap_transactions, invites. Okuma append-only; favori/silme son yazan kazanır, SUNUCU saati; cüzdan senkron değil, sunucu tek doğru.
(c) XCTest: project.yml `MoonlightTests: bundle.unit-test` + scheme testTargets; `xcodebuild test -scheme Moonlight -destination 'platform=iOS Simulator,name=iPhone 16'`. Worker: `node --test backend/test/`, aynı wallet-vectors.json. Beklenen değerler kaynaklı (USNO/NASA tabloları, yayınlanmış horary örnekleri); kaynaksız test yazılmaz.
(d) Prompt'lar Worker'da `backend/prompts/*.txt` (wrangler Text rules); cihaz yapılandırılmış girdi gönderir. Sonnet varsayılan; T05b'de Haiku/Sonnet karşılaştırması Damla'ya. Fable yok.
(e) Kullanımdan kalkanlar (dosya durur, çağrılmaz): CreditManager (T10b), ClaudeService (T18b), Element "%" (T05b), AstrologyService/MoonService USNO/sabit tutulma (T20), DailyReadingService LLM çağrısı (T20), BugunView (zaten ölü), ilişki/kariyer açılımı (T06). Asset katalogu dokunulmaz.
(f) Hesapsız + reinstall: /api/anon → sunucu anon + cüzdan, anon_id Keychain (ThisDeviceOnly). Günlük KH ve 4+1 sunucuda sayılır; reinstall aynı cüzdan. Cihaz sıfırlama günde 1 süreli KH kazandırır, kabul. DeviceCheck yalnız sayar (T30a); ret yok. Giriş = senkron + davet + web.

=====================================================================
5. COMPOUNDING ERROR — her R sonunda hakem yeniden koşar
=====================================================================
| Biten | Yeniden koşar |
|---|---|
| R1 | build; isimsiz → kapı; 3 tarot + 1 horary örneği isimli |
| R4 | R1 örnekleri: ilk cümle isim + cevap, sayım tek paragraf değil, kart kart paragraf yok, 3/5'te pozisyon etiketi yok; prompt'ta ay burcu + iç gezegen retro, "Plüton retro" yok, Element % yok; model Sonnet; test + node yeşil |
| R5 | R4 kontrolü 3/5/Kelt; ilişki/kariyer yok; ek kart önceki kartlara atıf; Kelt kesilmiyor |
| R6 | R4-R5 borç sonrası; vektörler Swift+JS; 14 coin'de pasif; 4. fal (horary dahil) +1; iade eski expires_at; DEBUG 500 yok; "Moonlight+" grep boş |
| R8 | R1-R6 ekranları yeni kabuktan; rozet kaymamış |
| R9 | çizgi; tarih testleri; R4 prompt'ta ay burcu SkyEngine = API (1 gün örnek) |
| R10 | R3 örneği yeniden: gösterge gezegenler + Ay'ın sonraki açısı + boş seyir bayrağı prompt'ta; chart yoksa istek yok; R4-R5 tarot örnekleri bozulmamış |
| R11 | R6 bakiye birleşme kuralına göre; anon kapandı; isim /api/me, ikinci kez sorulmuyor; iki cihaz geçmişi eşit; {{agenda}} dolu; 4+1 iki cihazda aynı; web'de aynı geçmiş |
| R12 | kart id'leri sayımla eşit; 78 kayıt dolu; Damla 5 kart okudu |
| R13-15 | Image(ad) kırık yok |
| R16 | R4-R5 örnekleri astro satırıyla; kombinasyon cümlesi kaybolmamış |
| R19 | curl 401/403; grep secret; SSV/IAP yolu (artık imzalı) geçiyor; metin ekranla tutarlı |
| R20 | R4-R5 örnekleri aynı kalitede |
Sabit: build + test + node yeşil; "yapma" grep (Plus, paylaşım kartı, pixel font, ilişki/kariyer, ters kart, seçmeli gündem, şarkı/kitap, 3/5 sabit pozisyon, "Golden Dawn").

=====================================================================
6. GERÇEK İŞ SIRASI
=====================================================================
1 T02 (R1) ── Faz 1 kapanış ──
2 K2a → 3 T01 → 4 T15 mock → 5 T03 → 6 T04 → 7 T05a → 8 T05b (R2 reels burada çekilir) → 9 T06 → 10 T07 → 11 T08 → 12 T09a → 13 T09b → 14 T10a → 15 T10b → 16 T11 → 17 K2b → 18 T12 → 19 T13 (R6 reels burada çekilir) → 20 T16 → 21 T17 → 22 T18a → 23 T18b (R10 reels burada çekilir) → 24 T14 (Damla "şimdi" derse) ── Faz 2 kapanış (Haiku/Sonnet kararı) ──
25 T19a → 26 T19b → 27 T20 → 28 T21 ── Faz 3 ──
29 K4 → 30 T22 → 31 T24 → 32 T25 → 33 T34 → 34 T26a → 35-38 T26b1-4 → 39 T27 → 40 T23a → 41 T23b (Damla "şimdi" derse) ── Faz 4 ──
42-44 T28a/b/c ── Faz 5 ── 45 T33 → R17 hakem → 46 T29 ── Faz 6 ── 47 T30a → 48 T30b → 49 T31 → 50 T32 ── Faz 7 ──

=====================================================================
SORULMAMIŞ AMA GÖRDÜKLERİM
=====================================================================
- Birim ekonomi: spec 9 hesapladı (uzun reklam Sonnet yorumunu ödemiyor, TR eCPM DOĞRULANMADI); ilk gerçek ölçüm T05b'nin 6 örneğinin token sayısı.
- 0809-kosu.md B R12 "şarkı/kitap" hâlâ yazıyor; A "bu turda yok".
- /api/claude açık proxy; T05a/T18b ile taşınır, T20'de DailyReadingService çağrısı kalkar, T30a'da uç kapanır. privacy-policy.html R6'dan itibaren yanlış; R19 öncesi TestFlight yok.
- T09b/T10b arası IAP makbuzu imzasız (decode + tekillik); JWS zinciri T30a. Risk kabul edildi, yazıldı.
- Kullanılmayan asset setleri (blink_* 32, glow_* 32, bg_full_moon_v2/3/5/6/7) ipa'yı şişiriyor; karar "dokunulmaz", boyut T31'de ölçülür.
- FreeAstrologyAPI /planets hız alanı DOĞRULANMADI; HoraryChartService "topocentric", SkyEngine geocentric → T18a sonrası fark kalkar.
- Görmediklerim: simülatör (plan turu); FreeAstrologyAPI ham JSON; Apple DeviceCheck dokümanı; horary örnek kitap sayfaları (T18a ajanı bulur, bulamazsa vaka yazılmaz).
