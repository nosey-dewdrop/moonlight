MOONLIGHT 0909 FAZ — gerçek iş planı (9 Eyl 2026, CTO)
Yasa: docs/0809-kosu.md A. Gösterim sırası B (R1..R21) DEĞİŞMEZ; aşağıda yalnız iş sırası değişir.
Spec: docs/0909-spec.txt (bölüm numaraları "spec N"). Satır referansları 9 Eyl koduna göre.
Boy: S ≤150 satır, M ≤500, L >500. Kanıt satırındaki "3 örnek" = D tablosuna kopyalanan 3 yorumun ilk 2 cümlesi.

=====================================================================
0. ÖNCE ÜÇ GERÇEK (planı belirleyen)
=====================================================================
- Sunucu tarafı bugün ÇALIŞMIYOR: backend/wrangler.toml:20 database_id = "REPLACE_WITH_D1_DATABASE_ID". D1 yaratılmadan cüzdan/üyelik/senkron uçlarının hiçbiri koşmaz. Damla'nın elle yapacağı iş (bkz. §7 S4).
- Worker'ın /api/claude ucu (worker.js:57,283-302) app-token'lı herkese açık proxy; token ipa içinde. Cüzdan sunucuda olacaksa yorum çağrısı da sunucuda borçlandırılmalı → prompt ve borç aynı uca taşınır (T05). Bu R4'te yapılır, R6'da borç eklenir; iki kere iş yok.
- Spec 4'ün "DeviceCheck zaman damgası aynı günse vermez" cümlesi YANLIŞ: Apple DeviceCheck last_update_time YYYY-AA (ay) çözünürlüklü, gün yok. Çözüm §4(f).

=====================================================================
1. R BAŞINA İŞ (gösterim sırası; "bağımlılık" = önce bitmesi gereken R/T)
=====================================================================
| R | Değişen / eklenen / kalkan | Boy | Bağımlılık | Risk (ne bozulur) | Kanıt |
|---|---|---|---|---|---|
| R1 Tarot ismiyle | TarotView.swift:432 revealCards, HoraryView.swift:153 askQuestion önüne isim kapısı; yeni Views/NameGateView.swift (BirthChartView.swift:312-350 isim alanı örnek alınır) | S | — | Kapı Horary'yi de tutar; isim boşsa yorum yok (spec 1 "isim zorunlu") | build; sim: isimsiz → kapı; 3 örnek yorumda isim geçiyor |
| R2 Gökyüzü yoruma girdi | Kodda var: HomeView.swift:163-196 olay listesi; ClaudeService.swift:53-55 Ay fazı+retro prompt'ta. İş: 0 | — | — | Element.swift:14-58 uydurma "%" değerleri de prompt'ta (:54) — R4'te kalkar | sim: Home olay listesi; prompt'ta "Ay fazı/Retrogradlar" satırı |
| R3 Horary | Kodda var: HoraryView + HoraryChartService + ClaudeService:90-145. İş: 0 (derinleşme T18, R4 bloğunda) | — | — | HoraryView:175 `try?` chart'sız devam ediyor; T18'de "chart yoksa istek yok" | sim: soru→cevap; D'ye 1 örnek |
| R4 Kombinasyon | T03 ReadingStore v2, T04 SpreadAnalysis, T05 prompt Worker'a, T18 horary göstergeler | L (4 alt) | T01 (test), Damla worker deploy | ClaudeService.tarotReading kalkar; TarotView call-site değişir; Element enerjileri prompt'tan çıkar | XCTest SpreadAnalysis; node test şablon; 3 örnek: "dağılım cümlesi" var mı, kart kart paragraf yok mu, isim var mı |
| R5 Anlamadım + açılımlar | T06 açılım UI (3/5/Kelt, ilişki/kariyer kalkar), T07 ek soru+ek kart | M+M | R4 (T05) | TarotView:183/425 "3 kart" sınırı açılıma göre; Kelt 10 kart + 400 kelime worker max_tokens 2048 tavanına yakın | sim: 3/5/10 seçilebiliyor; ek kart sonrası yorumda "ilk açılımda X yoktu" tipi cümle |
| R6 Coin + KH | T08 model+vektör, T09 D1+uçlar, T10 WalletManager, T11 cüzdan ekranı, T12 fiyat çağrıları, T13 AdMob, T14 davet | L (7 alt) | T01, D1 (Damla), AdMob hesabı (Damla) | CreditManager kalkar → 6 view derlenmez, tek seferde geçilir (T10+T11+T12 art arda); DEBUG 500 kredi (CreditManager:68-73) kalkar | XCTest+node vektör testi aynı JSON; sim: cüzdan ekranı, 15→1 butonu 14 coin'de pasif, 4. açılımda +1 süresiz KH |
| R7 Konvansiyon + mock | T15: mock/moonlight-mock.html (268 satır, var) 3 kalıpla yeniden; kod yok | S | — | Damla karar vermezse R8 başlamaz | Damla mock'a bakar, kalıp seçer |
| R8 Evren navigasyonu | T19a kabuk (ContentView.swift:23-43 TabView(.page) yerine), T19b geçişler; iskambil/Lenormand "yakında" yerleri | L (2 alt) | R7 kararı | Her sekmedeki CreditBadge/.padding(.top,54) yerleşimleri kayar; Settings/Wallet giriş noktaları | sim: 4 fal yeri görünür, tarot/horary/gökyüzü açılıyor |
| R9 Anasayfa gökyüzü | T16 SkyEngine ay, T17 gezegenler, T20 -14/+30 çizgi, T21 ay çizimi+widget | L (4 alt) | T16 önce T18'e de lazım (hız/uygulayan) | AstrologyService (sabit tutulma :136-188, +14 gün :10), MoonService USNO (:14-60) ve uydurma doğuş/batış (:99-106) kalkar; widget ayrı formül (MoonlightWidget.swift:17-50) → SkyEngine | XCTest: 2026-09-07 tutulma, bilinen dolunay tarihi ±1 sa; sim: -2 hafta/+1 ay çizgi; ay gölgesi aydınlanma yüzdesine göre |
| R10 Apple ile giriş | T22 istemci ASAuthorization + worker isim + anon→hesap birleştirme; T23 Google/e-posta | M + L | T09 (anon kimlik) | Apple ismi yalnız ilk yetkilendirmede verir; anında POST edilmezse kaybolur | sim: giriş → /api/me isimli; cüzdan aynı bakiye |
| R11 Profil + senkron | T24 readings/agenda uçları + kuyruk, T25 profil ekranı + konu çıkarımı | M+M | T03, T22 | Son yazan kazanır kuralı favori/silme için; okumalar append-only | iki simülatör aynı hesap: A'da fal, B'de geçmişte görünür |
| R12 Kart rehberi | T26 78 kart verisi JSON, T27 CardDetailView + "kaç kez geldi" | L + S | T03 (kart id'li geçmiş) | CardDetailView.swift:8-57 replace zinciri kalkar; TarotCard.name İngilizce anahtar olarak kalır | sim: karta tıkla → TR ad, arketip, sayı; "3 kez geldi" sayısı ReadingStore ile eşleşir |
| R13-R15 Resimler | T28a/b/c asset takası; blink_*(32 set), glow_*, bg_full_moon_v2..v7 ölü setler kalkar; .interpolation(.none) kalkar | S×3 | Damla çizer | Asset adı değişirse Image("…") kırılır; ad listesi docs/damla_tasks_moonlight.md ile birebir | build; sim ekran görüntüsü |
| R16-R17 | Faz 5 sonunda hakem belirler; plan yok | ? | — | — | — |
| R18 Burç/doğum haritası | Kodda VAR: BirthChartView.swift (448) + NatalChartService; SettingsView:101'den açılıyor. T29: navigasyona çıkar | S | R8 | BugunView.swift ölü dosya (hiçbir yerden çağrılmıyor) — silinir ya da R18'de kullanılır | sim: evrenden haritaya |
| R19 Güvenlik | T30a worker (CORS *, App Attest, IAP JWS zinciri, kullanıcı başına limit, /api/claude kalkar), T30b metinler (privacy-policy.html, KVKK aydınlatma+açık rıza, PrivacyInfo) | M+M | hepsi | privacy-policy.html "hesap yok/DB yok/reklam yok" → dördü yanlış | grep secret; curl yetkisiz 401; metinler Damla okur |
| R20 Hız | T31 ölçüm (açılış, chart, yorum gecikmesi, prompt token) sonra iyileştirme | S/M | R19 | Ölçmeden iyileştirme yok | sayılar D'ye |
| R21 Submit | T32: 1024 ikon, ASC kh1/3/10, credits5/10/15+plus satıştan kalkar, ekran görüntüleri, 4.3(b) review notu | S (Damla ağırlıklı) | R19, R20 | isim çakışması (rakipler.md: "Moonlight" tarot platformu) | ASC "Waiting for Review" |

=====================================================================
2. ALT GÖREVLER (her biri tek oturum, ≤300 satır değişiklik, ≤8 dosya)
=====================================================================
Girdi paketi = ajanın okuduğu dosyalar (satır). Her pakete 0809-kosu.md A (60 satır) + ilgili spec bölümü eklenir, sayılmadı.
| T | R | Ad | Girdi paketi (satır) | Çıktı | Kanıt | Hakem kontrol (A/spec cümlesi) |
|---|---|---|---|---|---|---|
| T01 | R6 | XCTest hedefi | project.yml(70), TarotCard(193), MoonPhase(57) → 320 | project.yml'e MoonlightTests target + scheme testTargets; MoonlightTests/SmokeTests.swift (78 kart, evre eşikleri) | `xcodebuild test` yeşil, 2 test | A "para kuralları ve yorum girdisi için XCTest (faz 2)" |
| T02 | R1 | İsim kapısı | TarotView(634), HoraryView(277), UserProfile(142), BirthChartView:300-448 → 1200 | NameGateView.swift; iki view'da kapı | sim; 3 örnek yorum isimli | A "İsmi soracağız"; spec 1 "isim yoksa yorum üretilmez" |
| T03 | R11/R12 | ReadingStore v2 | ReadingHistory(83), ReadingHistoryView(196), TarotView:573-613, HoraryView:153-204, SettingsView:505-520 → 420 | ReadingRecord: +cards[id], spread, sky, topic?, updatedAt, dirty; Application Support/readings.json; UserDefaults'tan tek seferlik taşıma; maxRecords kalkar | XCTest: kaydet/oku/taşı; sim: eski geçmiş duruyor | D notu "50 sınırı üyelik fazında kalkar"; spec 6 "readings.cards(json)'dan sayılır" |
| T04 | R4 | SpreadAnalysis | TarotCard(193), ClaudeService:36-86 → 250 | TarotCard: +number, +rank(court), +element; Models/SpreadAnalysis.swift (majör sayısı, element sayımı + dost/düşman çift, eksik takım, tekrar sayı, saray sayısı → hazır TR/EN cümleler); SpreadAnalysisTests | XCTest 8 vaka (spec 8 listesi) | spec 1 beş katman; A "kartların kombinasyon yorumu" |
| T05 | R4 | Tarot prompt Worker'a | ClaudeService(280), TarotView:573-613, worker.js(332), wrangler.toml(20), Localization:1-30 → 700 | backend/prompts/tarot.txt ({{name}} {{question}} {{cards}} {{analysis}} {{sky}} {{agenda}} {{language}}); backend/prompt.js; /api/reading/tarot (borç henüz yok); Services/ReadingAPI.swift; ClaudeService.tarotReading kalkar; Element enerjileri prompt'tan çıkar; backend/test/prompt.test.js (node --test) | node test; 3 örnek yorum | spec 1 "önce dağılımı söyle, sonra kartları bağla, ismi kullan"; A "ismini mutlaka kullan" |
| T06 | R5 | Açılım UI | TarotView(634), ClaudeService:20-34 → 670 | 3/5/Kelt seçici, seçim sınırı açılıma göre, ilişki+kariyer satırları (TarotView:206-207, ClaudeService:26-29) kalkar, pozisyon adları tek yerde | sim: 3 açılım | A "3 ve 5 kart açılımı, Kelt"; hakem "yapma" listesi ilişki/kariyer |
| T07 | R5 | Anlamadım | TarotView:444-498, ClaudeService:172-197, ReadingAPI, prompt.js → 400 | "Anlamadım" butonu → ek soru + 1 ek kart; prompts/tarot_followup.txt (önceki kartlar+yorum+ek soru+ek kart+aynı gökyüzü; ek kart element sayımına dahil); /api/reading/tarot-followup | node test; sim: ek kart yorumu | A "anlamadım → ek soru + ek kart, 3 coin"; spec 1 "ek kart pozisyonsuz" |
| T08 | R6 | Wallet modeli + vektörler | CreditManager(280) → 280 | Models/Wallet.swift (saf struct: coins, khTimed[expires], khPerm, fiyat tablosu, harcama sırası, 15→1, 4+1 sayacı, süre düşümü, iade aynı kova); MoonlightTests/wallet-vectors.json; WalletTests.swift; backend/wallet.js aynı vektörleri okur; backend/test/wallet.test.js | XCTest + node aynı 12 vektör yeşil | A sayıları: 1/2/3 KH, horary 1 KH, ek 3 coin, 15 coin, 24 sa, 4+1, reklam 1, davet 5; spec 8 para kuralları listesi |
| T09 | R6 | D1 v2 + anon + cüzdan uçları | worker.js(332), schema.sql(21), wallet.js → 400 | schema: users(+name,+anon,+invite_code,+invited_by,+daily_blocked), wallets, kh_timed, readings, agenda, iap_transactions; /api/anon (anon_id + JWT), /api/wallet GET, /wallet/convert, /wallet/iap, /reading/* uçlarına borç/iade/4+1 (atomik) | node test (miniflare yok: saf fonksiyonlar test edilir); curl akışı D'ye | spec 4 "sunucu doğrusu, cihaz önbellek"; "önce en erken sönecek süreli" |
| T10 | R6 | WalletManager | CreditManager(280), KeychainHelper(56), ReadingAPI → 400 | Services/WalletManager.swift (CreditManager silinir): Keychain anon_id, JWT, fetch/cache, optimistic düşüm; StoreKit ürünleri kh1/kh3/kh10 → /wallet/iap; Plus, credits5/10/15, hoş geldin 10, DEBUG 500 kalkar | build (T11-T12 ile aynı commit dizisi) | A "credits ve Moonlight+ kalkıyor"; "Mevcut kullanıcı yok, geçiş kuralı yok" |
| T11 | R6 | Cüzdan ekranı | NoCreditView(278), CreditBadge(53), SettingsView:258-410, WelcomeView(101), NotificationManager:103-118 → 700 | Views/WalletView.swift (coin, süreli KH + kalan süre, süresiz KH, 4+1 sayacı, "15 coin → 1 KH" butonu 15 altında pasif, "günlük KH al", davet kodu, KH paketleri); rozet coin+KH; Settings/Welcome/bildirim metinleri | sim ekran görüntüsü | A "cüzdan ekranı, günlük KH, 15 coin = süresiz KH, 4+1"; spec 4 "otomatik değil, buton" |
| T12 | R6 | Fiyat çağrı yerleri | TarotView, HoraryView, WalletManager → 950 | Tarot 1/2/3 KH, ek kart 3 coin, Horary 1 KH, ek soru 3 coin; bakiye yetmezse istek gitmez; hata → iade aynı kova | sim: 0 KH ile buton → cüzdan | A fiyatları; spec 4 "coin ile KH karışmaz" |
| T13 | R6 | AdMob | project.yml, Info.plist, WalletView, worker.js → 500 | GoogleMobileAds SPM; 2 rewarded ad unit (kısa +1 coin günde 3, uzun → günlük süreli KH); Services/RewardedAdManager.swift; /api/admob/ssv imza doğrulama → cüzdan; SKAdNetworkItems | sim test ad unit; SSV callback logu | A "reklam izleyerek 1 coin", "uzun reklam → günlük KH", 9 Eyl "kısa reklam günde 3", "ödül sunucu SSV ile" |
| T14 | R6 | Davet kodu | worker.js, WalletView → 400 | invite_code üretimi, /api/invite/redeem, arkadaşın ilk yorumu dönünce ikisine +5 (first_reading_done tek sefer), kendi kodu ret | node test; iki sim | A "arkadaş ilk falını bakınca ikisine de +5, sunucu doğrular" |
| T15 | R7 | Mock | mock/moonlight-mock.html(268), spec 7, ContentView(90) → 450 | Aynı HTML'de 3 kalıp: katmanlı sheet (Spotify/Arc), gökyüzü haritası (Zenly/saga), yatay odalar (Instagram); her birinde 4 fal yeri + gökyüzü çizgisi | Damla açar, seçer | A "evren hissi"; 9 Eyl "araştırma + mock, kararı Damla verir" |
| T16 | R9 | SkyEngine ay | project.yml, MoonService(150), MoonPhase(57), AstrologyService:37-82 → 350 | Packages/SkyEngine: CAstronomy (astronomy.c/.h vendored, MIT) + Swift hedef: illumination(date), phase(date), quarters(-14,+30), moonSign(date), moonIngresses; SkyEngineTests | XCTest: bilinen dördün/dolunay tarihleri ±1 sa, ay burcu değişimi | A "ayın durumuna göre şekli değişecek"; spec 3 "cihaz kazanır" |
| T17 | R9 | SkyEngine gezegenler | SkyEngine, HoraryChartService:44-61 → 400 | planetLongitude+speed, signIngress, retroStations(-14,+30), voidOfCourse(Ay), eclipses | XCTest: 2026-08-12 tam güneş, 2026-08-28 kısmi ay tutulması; bilinen Merkür retro tarihi | spec 3 olay listesi |
| T18a | R4 | HoraryFacts (Swift) | HoraryChartService(329), SkyEngine, ClaudeService:90-145 → 600 | Models/HoraryFacts.swift: yöneticiler tablosu, Yükselen/1. ev yöneticisi, Ay, her gösterge için uygulayan/ayrılan açı + orb + kalan derece (hızdan), Ay'ın sonraki açısı, boş seyir; evler API'den (yalnız /houses); planets/aspects API çağrısı kalkar | XCTest sabit chart 6 vaka | spec 2 "hazır hesaplanmış, ham derece dökümü değil"; "chart yoksa istek atılmıyor" |
| T18b | R4 | Horary prompt Worker'a | worker.js, prompt.js, HoraryView(277), HoraryFacts → 700 | prompts/horary.txt + horary_followup.txt; /api/reading/horary: küçük sınıflandırma çağrısı (kategori→ev, spec 2 tablosu backend/houses.js) sonra ana yorum; ek soru chart'ı görür; ClaudeService tümüyle kalkar | node test; 3 örnek | A "soruya göre cevap"; spec 2 kategori→ev tablosu |
| T19a | R8 | Evren kabuğu | ContentView(90), HomeView(316), Theme(180), MoonSceneView(177), seçilen mock → 900 | Seçilen kalıba göre kök view; 4 fal yeri (2 "yakında"); gökyüzü sahnesi arkada kalır; Settings/Wallet giriş noktaları | sim | A "UI/UX bir dünya, evren hissi"; "iskambil, Lenormand için yer" |
| T19b | R8 | Geçişler | T19a çıktısı, TarotView/HoraryView üst 60 satır → 700 | jest/animasyon; her fal ekranındaki üst boşluk/rozet konumu yeni kabuğa uyar | sim video | aynı |
| T20 | R9 | Gökyüzü çizgisi | AstrologyService(189), AstroEvent(63), HomeView(316), DailyReadingService(48), NotificationManager:120-150 → 750 | Services/SkyEvents.swift (-14/+30: evreler saatli, ay burç değişimi, boş seyir, gezegen geçişi, retro başlangıç/bitiş, tutulma); AstrologyService + MoonService USNO + sabit tutulma listesi kalkar; HomeView dikey çizgi "bugün" işaretli; günlük gökyüzü metni /api/daily-sky KV (gün×dil başına 1 çağrı) | sim: -2 hafta/+1 ay; XCTest olay sayısı | A "-2 hafta ve +1 ay"; "sadece gökyüzü durumları" |
| T21 | R9 | Ay çizimi + widget | MoonSceneView:167-176, HomeView:242-248, MoonlightWidget(170), NotificationManager:183-224 → 450 | Aydınlanmadan gölge maskesi (spec 3 elips terminatör) statik karakter üstüne; widget ve bildirim evre hesabı SkyEngine'e bağlanır (project.yml iki hedef) | sim: bugünkü aydınlanma ile gölge; widget aynı evre | A "ayın durumuna göre şekli değişecek" |
| T22 | R10 | Apple ile giriş | worker.js:76-171, schema, WalletManager, SettingsView:1-110 → 700 | Services/AuthManager.swift (ASAuthorizationController, fullName ilk yetkide anında POST), Views/SignInView.swift; worker: name sütunu, anon→user birleştirme (cüzdan + readings taşınır, anon satırı kapanır); isim yoksa kapı (T02) | sim: giriş → /api/me isim; cüzdan korunur | A "Apple ile giriş, isim zorunlu"; spec 5 "Apple ismi sadece ilk yetkilendirmede" |
| T23a | R10 | Google | worker.js:205-216, AuthManager → 400 | GoogleSignIn SPM; worker JWKS ile yerel doğrulama (tokeninfo kalkar); e-posta UNIQUE ile hesap birleştirme | sim | A "Giriş: Apple, Google, e-posta+şifre" |
| T23b | R10 | E-posta + şifre | worker.js, schema, SignInView → 500 | PBKDF2-SHA256 ≥600k; verify/reset token; Resend ile mail (Damla: alan adı + SPF/DKIM) | node test hash; sim akış | aynı; spec 5 e-posta bölümü |
| T24 | R11 | Senkron | ReadingStore, worker.js, schema → 600 | /api/readings (push dirty, pull since), /api/agenda; updated_at sunucu saati; son yazan kazanır (favori/silme), okuma satırı değişmez; çevrimdışı kuyruk | iki sim aynı hesap | A "iki cihazda aynı hesap"; spec 5 çakışma kuralı |
| T25 | R11 | Profil ekranı + konu | SettingsView(520), BirthChartView:300-448, worker reading uçları → 900 | Views/ProfileView.swift: isim, burç, gündem (son 5 soru), sık konular (readings.topic — worker her yorumdan sonra 1 küçük çağrı ile 1 kelimelik konu çıkarır), fal geçmişi, cüzdan; gündem prompt'a {{agenda}} olarak girer | sim; 3 örnek: gündem cümlesi yorumda | A üyelik paragrafı; 9 Eyl "sık konular: LLM sorudan çıkarır", "gündem: serbest metin, API'ye gider, birikir" |
| T26 | R12 | Kart verisi | TarotCard(193), CardDetailView:59-64,206-244 → 260 | Resources/cards.json 78×{id, name_tr, keywords_tr/en, meaning_tr/en, archetype, number, element}; TarotCard JSON'dan yüklenir (id/name sabit) | XCTest: 78 kayıt, boş alan yok | A "tarot rehberi gibi"; 9 Eyl "ters kart yok", "şarkı/kitap bu turda yok" |
| T27 | R12 | Kart sayfası + sayım | CardDetailView(245), ReadingStore, TarotView:243-288 → 600 | replace zinciri kalkar; arketip/sayı/element bölümleri; "bu kart sende N kez geldi, son: tarih, en sık: açılım" | sim | spec 6 "kaç kez geldi" |
| T28a/b/c | R13/14/15 | Asset takası | Assets.xcassets adları, damla_tasks_moonlight.md(177), MoonSceneView, HomeView, TarotView kart hücresi → 400 | a: ay karakterleri + gökyüzü; b: 78 kart + card_back/frame; c: ikonlar + kedi + app icon 1024; ölü setler kalkar; .interpolation(.none) kalkar (watercolour) | build; ekran görüntüsü | damla_tasks "pixel artı bırakıyoruz"; PROJECT.md "pixel font yok" |
| T29 | R18 | Harita evrene | BirthChartView(448), T19a kabuğu, BugunView(213 ölü) → 900 | Doğum haritası evrenden ulaşılır; BugunView silinir ya da R18 ekranı olur (hakem) | sim | A "BELKİ burç / doğum haritası" |
| T30a | R19 | Worker sertleştirme | worker.js, wrangler.toml → 500 | CORS web alanına; App Attest; IAP JWS Apple kök sertifika zinciri; kullanıcı başına limit; /api/claude ve /api/astrology/planets,aspects kalkar; secret taraması | curl 401/403; grep | CLAUDE.md §6 "secret sızmaz" |
| T30b | R19 | Metinler | docs/privacy-policy.html, terms.html, PrivacyInfo.xcprivacy, SettingsView:411-500 → 700 | KVKK aydınlatma + ayrı açık rıza (2026/347), yurt dışı aktarım (Cloudflare/Anthropic/Google/AdMob), sunucudan silme ucu + buton | Damla okur; sim silme → /api/me 404 | spec 5 KVKK |
| T31 | R20 | Hız | tüm servisler → ölçüm | açılış süresi, /houses gecikmesi, yorum gecikmesi, prompt token sayısı; sonra en büyük 2 kalem | sayılar önce/sonra | A "ölçüm, sonra iyileştirme" |
| T32 | R21 | Submit | PROJECT.md, ASC → — | ikon, ürünler, ekran görüntüleri, review notu (kombinasyon motoru + cihazda efemeris + horary = 4.3(b) farkı) | ASC durumu | — |
Toplam: 39 alt görev (T01-T32, T18/T19/T23/T30 ikiye, T28 üçe).

=====================================================================
3. GÖSTERİM SIRASI ↔ İŞ SIRASI ÇAKIŞMALARI ve KARARLAR
=====================================================================
| Çakışma | Karar | Gerekçe |
|---|---|---|
| R6 cüzdan cihazda, R10-11 sunucuda → iki kere cüzdan | Cüzdan İLK GÜNDEN sunucuda: hesapsız kullanıcı da /api/anon ile sunucu kimliği alır (anon_id Keychain'de), aynı JWT şeması; R10'da anon satırı hesaba birleşir | Spec 4 "sunucu doğrusu" zaten diyor; Keychain'de para tutup sonra taşımak = T10'un iki kez yazılması. Bedel: D1 R6'dan önce hazır olmalı (§7 S4) |
| Tarot prompt cihazda mı sunucuda mı (R4) | Sunucuda (T05). Cihaz yapılandırılmış girdi gönderir (kartlar + SpreadAnalysis cümleleri + gökyüzü + isim), şablon Worker'da | Borç ile yorum aynı uçta atomik olmalı; prompt ipa'dan sökülemez; web okuması (A) aynı ucu kullanır. XCTest yine cihazda: girdi üreticisini test eder |
| Horary derinleşmesi (spec 2) B'de reels'i yok | R4'e bağlanır (T18): R4 reels "yorum kombinasyon/gösterge okuyor" ikisini gösterir; hakem faz 2 sonunda reels metnini günceller | Bağlanmayan iş yapılmaz kuralı; R3 zaten çekilmiş olacak |
| Horary uygulayan/ayrılan için gezegen hızı lazım; hız FreeAstrologyAPI'de yok (DOĞRULANMADI) | SkyEngine (T16-T17, R9 işi) R4 bloğuna çekilir, T18'den önce | Aynı kütüphane iki yerde lazım; API'ye yalnız /houses kalır (3 çağrı → 1, worker.js:37 limitine rahatlama) |
| Kart id'li geçmiş R12'de lazım, ama fallar R4'ten itibaren üretiliyor | ReadingStore v2 (T03) R4 bloğunun başına | Aksi halde R12'de "kaç kez geldi" boş başlar |
| R7 mock Damla kararı bekliyor, R8 ona bağlı | T15 mock'u iş sırasında en öne (T02'den hemen sonra) | Damla karar süresi kritik yolda olmasın |
| R13-15 resimler Damla'da | Kod işi asset adı korunursa S; sırada bağımsız, paralel | — |
| Widget ayrı ay formülü (MoonlightWidget:17-50) | T21'de SkyEngine'e bağlanır | Aksi halde evre sınırında uygulama ve widget farklı ay gösterir |
Önce altyapı (gösterimde görünmez ama bağlı): T01 test hedefi → R6; T03 veri katmanı → R11/R12; T05 prompt katmanı → R4; T09 D1 → R6; T16-17 efemeris → R9 (R4 bloğunda kurulur).

=====================================================================
4. ALTYAPI KARARLARI (3'er satır)
=====================================================================
(a) Efemeris: astronomy-engine tek C dosyası (astronomy.c/.h, MIT) `Packages/SkyEngine/Sources/CAstronomy/` altına kopyalanır; SwiftPM C hedefi module map'i kendisi üretir, köprü yazılmaz. Üstüne `SkyEngine` Swift hedefi (date→Astronomy_Time, boylam/30 = burç, arama fonksiyonlarıyla olay tarama). project.yml: `packages: SkyEngine: path: Packages/SkyEngine`, Moonlight ve MoonlightWidget hedeflerinde dependency. Swift portu yok, SwiftAA (AA+ kaynak kısıtı) ve Swiss Ephemeris (AGPL) kullanılmaz.
(b) Veri: UserDefaults → Application Support altında Codable JSON dosyaları (readings.json, agenda.json, wallet-cache.json) tek `ReadingStore` actor'ü arkasında; SwiftData/GRDB yok (bağımlılık/makro maliyeti, 8 GB makine, birkaç bin kayıt için gereksiz). D1: users, wallets, kh_timed, readings(id, user_id, type, spread, question, cards json, reading, sky json, topic, created_at, updated_at, deleted), agenda, iap_transactions, invites. Senkron: okuma satırı append-only (çakışma yok); favori/silme "son yazan kazanır", zaman damgası SUNUCU saati (cihaz saatine güvenilmez); cüzdan asla senkron değil, sunucu tek doğru, cihaz önbellek + optimistic düşüm, hata → sunucudan yeniden çek.
(c) XCTest: project.yml'e `MoonlightTests: type: bundle.unit-test, sources: MoonlightTests, dependencies: [target: Moonlight]` + `schemes: Moonlight: test: targets: [MoonlightTests]`; xcodegen sonra `xcodebuild test -scheme Moonlight -destination 'platform=iOS Simulator,name=iPhone 16'`. İlk testler: 78 kart, evre eşikleri (T01); sonra SpreadAnalysis 8 vaka (T04), Wallet 12 vektör (T08), SkyEngine bilinen tarihler (T16-17), HoraryFacts sabit chart (T18a). Worker tarafı `node --test backend/test/` (bağımlılık yok), aynı wallet-vectors.json'u okur.
(d) Prompt'lar: Worker'da `backend/prompts/*.txt` düz metin şablon ({{alan}}), wrangler `rules = [{type="Text", globs=["prompts/*.txt"]}]` ile import; `backend/prompt.js` doldurur. Hakem ve Damla prompt'u kodsuz okur; cihazda prompt metni yok. Cihaz yalnız yapılandırılmış girdi üretir (SpreadAnalysis, HoraryFacts, gökyüzü, isim, gündem) → XCTest bunu test eder.
(e) Kalkacaklar: CreditManager tamamen (Plus, credits5/10/15, hoş geldin 10, günlük 3, DEBUG 500) → WalletManager (T10). İlişki/kariyer açılımı (TarotView:206-207, ClaudeService:26-29) → T06. DailyReadingService KALIR (A "şu anda gökyüzünde ne var" kartı) ama T20'de /api/daily-sky KV ucuna taşınır (kullanıcı başı değil gün×dil başı 1 çağrı; açık /api/claude kapanır). MoonlightWidget kalır, T21'de SkyEngine'e bağlanır. BugunView ölü, T29'da karar. Element.swift "enerji %" değerleri T05'te prompt'tan çıkar, dosya T20'de silinir.
(f) Hesapsız + DeviceCheck (Worker): uygulama ilk açılışta DCDevice token'ıyla POST /api/anon → Worker Apple DeviceCheck'e (ES256 JWT, .p8/Key ID/Team ID secret) query_two_bits sorar. bit0=0 → anon kullanıcı yaratılır, bit0=1 yazılır, anon_id Keychain'e (ThisDeviceOnly; silme-yükleme sonrası kalır, garanti değil). bit0=1 ve Keychain boş (cihaz sıfırlanmış/tüm içerik silinmiş) → anon yaratılır ama daily_blocked=1: günlük KH ve hoş geldin yok, hesap açınca açılır. Gün çözünürlüğü olmadığı için "aynı gün" kontrolü yerine "bu cihazda daha önce anon cüzdan açıldı" kontrolü; dürüst ve üyeliğe iter. Secret yoksa DeviceCheck atlanır (geliştirme), üretimde zorunlu (T30a).

=====================================================================
5. COMPOUNDING ERROR — her R sonunda hakemin yeniden koşacağı önceki kanıtlar
=====================================================================
| Biten | Yeniden koşar |
|---|---|
| R1 | build; isimsiz → kapı; 3 tarot + 1 horary örneği isimli |
| R4 | R1 örnekleri yeniden üretilir: isim var mı, "dağılım" cümlesi ilk paragrafta mı, kart kart paragraf yok mu; R2: prompt'ta ay evresi + ay burcu + iç gezegen retro var, "Plüton retro" yok, Element % yok; `xcodebuild test` + `node --test` |
| R5 | R4 kombinasyon kontrolü 3/5/Kelt için tekrar; ilişki/kariyer ekranda yok; ek kart yorumu önceki kartlara atıf yapıyor; Kelt yorumu 2048 token'da kesilmiyor |
| R6 | R4-R5 örnekleri yeniden (borç sonrası akış); wallet vektörleri Swift+JS; 14 coin'de buton pasif; 4. açılımda kh_perm+1; iade eski expires_at ile; DEBUG'da 500 kredi yok; Plus metni hiçbir ekranda yok (grep "Moonlight+") |
| R8 | R1-R6 ekranları yeni kabuktan açılıyor; rozet/üst boşluk kaymamış; R6 cüzdan ekranı erişilir |
| R9 | R2 olay listesi yerine çizgi; bilinen tarih testleri; widget = uygulama evresi; R4 prompt'ta ay burcu artık SkyEngine'den ve API ile aynı burcu veriyor (1 gün örnek) |
| R10 | R6 cüzdan bakiyesi girişten sonra aynı; anon satırı kapandı; isim /api/me'de; R1 kapısı hesaplı kullanıcıda da çalışıyor |
| R11 | R10; iki cihaz geçmişi eşit; R4 prompt'ta {{agenda}} dolu; R6 4+1 sayacı iki cihazda aynı |
| R12 | R11 geçmiş kart id'leri sayımla eşleşiyor; R5 kart adları TR (replace zinciri yok); 78 kayıt boş alan yok |
| R13-15 | R8-R9 ekranları yeni assetlerle; Image(ad) kırık yok (build + sim tüm ekranlar) |
| R19 | curl 401/403; grep secret; R6 SSV/IAP yolu hâlâ geçiyor; privacy metni ekranla tutarlı |
| R20 | ölçüm sonrası R4-R5 örnekleri aynı kalitede (model/token değişmediyse) |
Her R'de sabit: `xcodebuild build` + `xcodebuild test` + `node --test` yeşil; hakem "yapma" listesi (Plus, paylaşım kartı, pixel font, ilişki/kariyer, ters kart, seçmeli gündem, şarkı/kitap) grep ile.

=====================================================================
6. GERÇEK İŞ SIRASI (tek liste)
=====================================================================
1 T01 (R6) → 2 T02 (R1) → 3 T15 mock (R7, Damla karar süresi başlar) → 4 T03 (R11/R12) → 5 T04 (R4) → 6 T05 (R4) → 7 T06 (R5) → 8 T07 (R5) →
9 T08 (R6) → 10 T09 (R6, D1 hazır olmalı) → 11 T10 (R6) → 12 T11 (R6) → 13 T12 (R6) → 14 T13 (R6, AdMob hesabı) → 15 T14 (R6) →
16 T16 (R9) → 17 T17 (R9) → 18 T18a (R4) → 19 T18b (R4) →
20 T19a (R8, mock kararı) → 21 T19b (R8) → 22 T20 (R9) → 23 T21 (R9) →
24 T22 (R10) → 25 T24 (R11) → 26 T25 (R11) → 27 T23a (R10) → 28 T23b (R10, Resend/alan adı) →
29 T26 (R12) → 30 T27 (R12) → 31-33 T28a/b/c (R13-15, Damla çizince; paralel) → 34 T29 (R18) → R16/R17 hakem →
35 T30a (R19) → 36 T30b (R19) → 37 T31 (R20) → 38 T32 (R21).
Faz kapanışları (Damla simülatörde bakar): Faz 1 = 2 sonrası; Faz 2 = 19 sonrası (R4 horary örnekleri dahil); Faz 3 = 23; Faz 4 = 30; Faz 5 = 33; Faz 6 = 34+; Faz 7 = 38.

=====================================================================
7. KURUCUYA SORULAR (en fazla 5; cevapsızsa varsayılan)
=====================================================================
S1. Astrolojik karşılık katmanı (kart → gezegen/burç) yoruma ve rehbere girsin mi? Varsayılan: hayır; T04/T26 alan bırakır, metin girmez.
S2. 5 kart pozisyonları koddaki "Geçmiş / Şimdi / Gizli / Tavsiye / Sonuç" mu kalsın, "Durum / Engel / Tavsiye / Gizli etki / Sonuç" mu olsun? Varsayılan: koddaki kalır.
S3. DeviceCheck gün bilgisi vermediği için, cihazı sıfırlayıp yeniden kuran hesapsız kullanıcı hesap açana kadar günlük KH almasın — kabul mü? Varsayılan: evet (§4f).
S4. Senin elle yapacağın 5 kurulum — D1 create + database_id, AdMob hesabı + 2 ad unit, DeviceCheck .p8 anahtarı, App Store Connect kh1/kh3/kh10, Resend + alan adı DNS — hangileri R6 başlamadan (iş sırası 10) hazır olur? Varsayılan: D1 hazır değilse T09-T14 beklemez, önce T16-T18 koşar; AdMob yoksa T13 atlanır ve günlük KH reklamsız verilir (geçici, hakem sapma yazar).
S5. Worker'da model claude-haiku-4-5 sabit (worker.js:288); kombinasyon ve horary yorumu için aynı prompt'la haiku ve sonnet'ten 3'er örnek üretip sen okuduktan sonra mı seçelim? Varsayılan: evet, T05 sonunda 6 örnek D'ye, karar senin; o ana kadar haiku.

=====================================================================
SORULMAMIŞ AMA GÖRDÜKLERİM
=====================================================================
- DeviceCheck last_update_time ay çözünürlüklü; spec 4 satır 93'ün "aynı günse vermez" cümlesi uygulanamaz. Spec bu satırda düzeltilmeli (bu dosya A'yı ve spec'i değiştirmez, hakem not düşsün).
- D1 yaratılmamış (wrangler.toml:20). Bugün /api/auth/apple, /api/me, /api/profile çağrılırsa env.DB yok → 500 (worker.js:66-68 "Internal error").
- /api/claude app-token'la açık proxy; token ipa'dan çıkarılabilir; Claude bütçesi dışarıdan yakılabilir. T05/T18b ile kapanır, T30a'da silinir.
- BugunView.swift (213 satır) hiçbir yerden çağrılmıyor ama derleniyor; içinde isimli selamlama var. BirthChartView + NatalChartService gerçek doğum haritası hesaplıyor (R18 için hazır zemin).
- Assets: 78 tarot seti tam; 32 blink_* + glow_* + bg_full_moon_v2..v7 ölü set (damla_tasks "eski 64 frame iptal"); repo boyunu şişiriyor, T28'de silinir.
- Repo kökünde Next.js sitesi (app/, package.json next 16.1.6, node_modules) ve mock/moonlight-mock.html var; landing/web okuma için altyapı hazır ama plan dışı.
- HoraryChartService "topocentric" (spec zaten notlu); SkyEngine geocentric hesaplar → T18'den sonra fark kalkar. FreeAstrologyAPI /planets çıktısında hız alanı var mı bakılmadı (DOĞRULANMADI); yoksa uygulayan/ayrılan yalnız SkyEngine ile mümkün.
- EngagementPrompts.swift ilk yorumdan sonra App Store puan isteği, ikincide bildirim izni; A'da yok, dokunulmadı. NotificationManager:103-118 "krediler yenilendi 11:00" bildirimi T11'de "günlük KH hazır" olur.
- GoogleMobileAds SPM paketi büyük; 8 GB makinede ilk derleme belirgin uzar; T13 tek başına oturum.
- Secrets.swift .gitignore'da, iyi. Worker rate limit IP başına 30/dk; SkyEngine sonrası chart 3→1 çağrı düşünce sorun azalır.
- Görmediklerim: simülatörü çalıştırmadım (plan turu); FreeAstrologyAPI ham JSON'unu görmedim; Apple DeviceCheck dokümanı bu turda açılmadı, ay çözünürlüğü bilgim önceki bilgiden (Damla'ya söylemeden önce T09 ajanı Apple sayfasından doğrulasın).
