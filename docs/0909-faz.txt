MOONLIGHT 0909 FAZ — gerçek iş planı (9 Eyl 2026, CTO; hakem turu 1 sonrası düzeltildi)
Yasa: docs/0809-kosu.md A (9 Eyl kararları dahil, A:60-78). Gösterim sırası B (R1..R21) DEĞİŞMEZ; aşağıda yalnız iş sırası değişir.
Spec: docs/0909-spec.txt ("spec N"). Satır referansları 9 Eyl koduna göre.
Boy: S ≤150 satır, M ≤500, L >500; alt görev hedefi ≤300 satır, ≤8 dosya. "3 örnek" = D tablosuna kopyalanan 3 yorumun ilk 2 cümlesi.
Kararlar (9 Eyl, koordinatör): resim/asset dosyalarına dokunulmaz; kod dosyaları (BugunView, Element, USNO, sabit tutulma listesi) kullanımdan kaldırılır, silinmez. DailyReadingService LLM çağrısı kalkar, kart olaylardan şablon cümle üretir. Günlük KH hesapsıza da gelir; giriş = bulut senkron + davet + web.

=====================================================================
0. ÖNCE ÜÇ GERÇEK (planı belirleyen)
=====================================================================
- Sunucu tarafı bugün ÇALIŞMIYOR: backend/wrangler.toml:20 database_id = "REPLACE_WITH_D1_DATABASE_ID"; env.DB yok → auth/me/profile uçları 500 (worker.js:66-68). D1 yaratılması faz 2'nin ilk alt görevi (K2).
- Worker'ın /api/claude ucu (worker.js:57,283-302) app-token'lı herkese açık proxy; token ipa içinde. Cüzdan sunucuda olacaksa yorum çağrısı da sunucuda borçlandırılır → prompt ve borç aynı uca (T05, borç T09b). İki kere iş yok.
- Worker deploy: ajan `npx wrangler deploy` ile yapar; Damla K2'de bir kez `npx wrangler login` verir. 10+ deploy (T05,T07,T09a/b,T13,T14,T18b,T22,T23a/b,T24,T30a) Damla'yı beklemez. Model: worker.js:288 haiku sabiti T05'te Sonnet olur (A: "Sonnet varsayılan").

=====================================================================
1. R BAŞINA İŞ (gösterim sırası; "bağımlılık" = önce bitmesi gereken)
=====================================================================
| R | Değişen / eklenen / kullanımdan kalkan | Boy | Bağımlılık | Risk (ne bozulur) | Kanıt |
|---|---|---|---|---|---|
| R1 Tarot ismiyle | TarotView.swift:432 revealCards, HoraryView.swift:153 askQuestion önüne isim kapısı; yeni Views/NameGateView.swift (BirthChartView.swift:312-350 isim alanı örnek) | S | — | Kapı Horary'yi de tutar; isim boşsa yorum yok (spec 1) | build; sim: isimsiz → kapı; 3 örnek yorumda isim |
| R2 Gökyüzü yoruma girdi | Kodda var: HomeView.swift:163-196 olay listesi; ClaudeService.swift:53-55 Ay fazı+retro prompt'ta. İş: 0 | — | — | Element.swift:14-58 uydurma "%" değerleri prompt'ta (:54) — T05'te prompt'tan çıkar | sim: Home olay listesi; prompt'ta "Ay fazı/Retrogradlar" |
| R3 Horary | Kodda var: HoraryView + HoraryChartService + ClaudeService:90-145. İş: 0 (derinleşme T18, R4 bloğunda) | — | — | HoraryView:175 `try?` chart'sız devam; T18'de "chart yoksa istek yok" | sim: soru→cevap; 1 örnek |
| R4 Kombinasyon | T03 ReadingStore v2, T04 SpreadAnalysis, T05 prompt Worker'a + Sonnet, T18a/b horary göstergeler | L (5 alt) | K2, T01 | ClaudeService.tarotReading kalkar; TarotView call-site; 3/5 kartta pozisyon adı prompt'a girmez (A:9 Eyl) | XCTest SpreadAnalysis; node test şablon; 3 örnek: dağılım cümlesi var, kart kart paragraf yok, isim var; Haiku vs Sonnet aynı 3 açılım Damla'ya |
| R5 Anlamadım + açılımlar | T06 açılım UI (3/5/Kelt, ilişki/kariyer kalkar, 3/5'te pozisyon etiketi yok), T07 ek soru+ek kart | M+M | T05 | TarotView:183/425 "3 kart" sınırı açılıma göre; Kelt max_tokens (T05'te açılıma göre) | sim: 3/5/10 seçilebiliyor; ek kart yorumu "ilk açılımda X yoktu" tipi cümle |
| R6 Coin + KH | K2 kurulum, T01 test, T08 model+vektör, T09a/b D1+uçlar, T10a/b WalletManager, T11 cüzdan ekranı, T12 fiyat çağrıları, T13 AdMob, T14 davet | L (11 alt) | K2 | CreditManager'ı 6 view okuyor → T10a ince adaptörle sarar, build hiç kırılmaz; DEBUG 500 (CreditManager:68-73) kalkar | XCTest+node aynı vektörler; sim: cüzdan ekranı, 15→1 butonu 14 coin'de pasif, 4. fal (horary dahil) +1 süresiz KH |
| R7 Konvansiyon + mock | T15: mock/moonlight-mock.html (268 satır, var) 3 kalıpla; kod yok | S | — | Damla karar vermezse R8 başlamaz | Damla mock'a bakar, kalıp seçer |
| R8 Evren navigasyonu | T19a kabuk (ContentView.swift:23-43 TabView(.page) yerine), T19b geçişler; iskambil/Lenormand "yakında" | L (2 alt) | R7 kararı | Her sekmedeki CreditBadge/.padding(.top,54) kayar; Settings/Wallet giriş noktaları | sim: 4 fal yeri, tarot/horary/gökyüzü açılıyor |
| R9 Anasayfa gökyüzü | T16 SkyEngine ay, T17 gezegenler, T20 -14/+30 çizgi, T21 ay çizimi+widget | L (4 alt) | T16 T18a'ya da lazım | AstrologyService (+14 gün :10, sabit tutulma :136-188) ve MoonService USNO (:14-60, uydurma doğuş/batış :99-106) kullanımdan kalkar, silinmez; widget ayrı formül (MoonlightWidget.swift:17-50) → SkyEngine | XCTest: 2026-03-03 ve 2026-08-28 ay tutulmaları, 2026-08-12 güneş, bilinen dolunay ±1 sa; sim: -2 hafta/+1 ay çizgi; ay gölgesi aydınlanmaya göre |
| R10 Apple ile giriş | K4 kurulum, T22 ASAuthorization + worker isim + anon→hesap; T23a Google, T23b e-posta | M+L | T09a | Apple ismi yalnız ilk yetkide gelir; anında POST | sim: giriş → /api/me isimli; cüzdan aynı bakiye |
| R11 Profil + senkron | T24 readings/agenda uçları + kuyruk, T25 profil + konu çıkarımı | M+M | T03, T22 | Son yazan kazanır yalnız favori/silme | iki sim aynı hesap: A'da fal, B'de geçmişte |
| R12 Kart rehberi | T26a/b 78 kart verisi JSON, T27 CardDetailView + "kaç kez geldi" | M+M+S | T03 | CardDetailView.swift:8-57 replace zinciri kalkar; TarotCard.name İngilizce anahtar kalır | sim: karta tıkla → TR ad, arketip, sayı; "N kez geldi" ReadingStore ile eşit |
| R13-R15 Resimler | T28a/b/c asset takası (Damla'nın yeni dosyaları eklenir, mevcut asset dosyalarına dokunulmaz); .interpolation(.none) kalkar | S×3 | Damla çizer | Asset adı değişirse Image("…") kırılır; adlar docs/damla_tasks_moonlight.md ile birebir | build; ekran görüntüsü |
| R16 Astrolojik karşılık | T33: kart → gezegen/burç katmanı yorumda + rehberde (A: faz 6, App Store öncesi) | M | T04, T26a | Prompt uzar; Kelt token tavanı | 3 örnek: gezegen/burç cümlesi; rehberde alan dolu |
| R17 | Faz 5 sonunda hakem belirler | ? | — | — | — |
| R18 Burç/doğum haritası | Kodda VAR: BirthChartView.swift (448) + NatalChartService; SettingsView:101'den. T29 evrene çıkarır | S | R8 | BugunView.swift ölü (çağrılmıyor); kullanımdan kalkmış sayılır, silinmez | sim: evrenden haritaya |
| R19 Güvenlik | T30a worker (CORS *, App Attest, IAP JWS zinciri, kullanıcı başına limit, /api/claude kapanır), T30b metinler | M+M | hepsi | privacy-policy.html "hesap/DB/reklam yok" → R6'dan itibaren yanlış; R19 öncesi TestFlight dağıtılmaz | curl 401/403; grep secret; Damla okur |
| R20 Hız | T31 ölçüm, sonra iyileştirme | S/M | R19 | Ölçmeden iyileştirme yok | sayılar D'ye |
| R21 Submit | T32: 1024 ikon, ekran görüntüleri, credits5/10/15+plus satıştan kaldırma, 4.3(b) notu | S | R19, R20 | isim çakışması (rakipler.md "Moonlight" tarot platformu) | ASC "Waiting for Review" |

=====================================================================
2. ALT GÖREVLER (tek oturum, ≤300 satır, ≤8 dosya)
=====================================================================
Girdi paketi = ajanın okuduğu dosyalar (satır); her pakete A (78 satır) + ilgili spec bölümü eklenir, sayılmadı.
| T | R | Ad | Girdi paketi (satır) | Çıktı | Kanıt | Hakem kontrol (A/spec cümlesi) |
|---|---|---|---|---|---|---|
| K2 | R6 | Faz 2 elle kurulum (Damla) | backend/README.md, wrangler.toml(20), schema.sql(21) → 60 | Ajan ekran ekran tarif eder, Damla yapar: `wrangler login`; `wrangler d1 create moonlight_db` + database_id; AdMob hesabı + 2 rewarded ad unit; DeviceCheck .p8 (Key ID, Team ID) secret; ASC kh1/kh3/kh10 consumable; secret'lar `wrangler secret put` | `wrangler d1 execute --remote` çalışır; `wrangler deploy` ajan elinden geçer | A "elle kurulumlar: her fazın ilk alt görevi, ajan ekran ekran tarif eder" |
| T01 | R6 | XCTest hedefi | project.yml(70), TarotCard(193), MoonPhase(57) → 320 | MoonlightTests target + scheme testTargets; SmokeTests.swift (78 kart, evre eşikleri) | `xcodebuild test` yeşil | A "XCTest (faz 2'de kurulur)" |
| T02 | R1 | İsim kapısı | TarotView(634), HoraryView(277), UserProfile(142), BirthChartView:300-448 → 1200 | NameGateView.swift; iki view'da kapı | sim; 3 örnek isimli | A "İsmi soracağız"; spec 1 "isim yoksa yorum üretilmez" |
| T03 | R11/R12 | ReadingStore v2 | ReadingHistory(83), ReadingHistoryView(196), TarotView:573-613, HoraryView:153-204, SettingsView:505-520 → 420 | ReadingRecord: +cards[id], spread, sky, topic?, updatedAt, dirty; Application Support/readings.json; UserDefaults'tan taşıma; maxRecords kalkar | XCTest kaydet/oku/taşı; sim eski geçmiş duruyor | D notu "50 sınırı kalkar"; spec 6 "readings.cards'tan sayılır" |
| T04 | R4 | SpreadAnalysis | TarotCard(193), ClaudeService:36-86 → 250 | TarotCard: +number, +rank, +element, +astro (boş, T33 doldurur); Models/SpreadAnalysis.swift (majör sayısı, element sayımı + dost/düşman çift, eksik takım, tekrar sayı, saray sayısı → hazır cümleler); testler | XCTest 8 vaka | spec 1 beş katman; A "kartların kombinasyon yorumu" |
| T05 | R4 | Tarot prompt Worker'a + Sonnet | ClaudeService(280), TarotView:573-613, worker.js(332), wrangler.toml, Localization:1-30 → 700 | backend/prompts/tarot.txt ({{name}} {{question}} {{spread}} {{cards}} {{analysis}} {{sky}} {{agenda}} {{language}}); 3/5 kartta pozisyon adı YOK, Kelt 10 pozisyon var (ClaudeService:20-34 positionNames yalnız Kelt); backend/prompt.js; /api/reading/tarot; max_tokens açılıma göre (Kelt tavanı 2048'in üstüne); model Sonnet; ReadingAPI.swift; Element "%" prompt'tan çıkar; backend/test/prompt.test.js | node test; aynı 3 açılım Haiku vs Sonnet 6 örnek D'ye, Damla seçer | A "3 ve 5 kartta sabit pozisyon adı yok; Kelt kalır"; "Sonnet varsayılan"; spec 1 "önce dağılımı söyle" |
| T06 | R5 | Açılım UI | TarotView(634) → 640 | 3/5/Kelt seçici; seçim sınırı açılıma göre; ilişki+kariyer (TarotView:206-207, ClaudeService:26-29) kalkar; 3/5'te pozisyon etiketi yok, Kelt'te var | sim 3 açılım | A "3 ve 5 kart, Kelt"; "sabit pozisyon adı yok"; hakem "yapma" ilişki/kariyer |
| T07 | R5 | Anlamadım | TarotView:444-498, ClaudeService:172-197, ReadingAPI, prompt.js → 400 | "Anlamadım" → ek soru + 1 ek kart; prompts/tarot_followup.txt (önceki kartlar+yorum+ek soru+ek kart+aynı gökyüzü; ek kart element sayımına dahil); /api/reading/tarot-followup | node test; sim | A "anlamadım → ek soru + ek kart, 3 coin"; spec 1 "ek kart pozisyonsuz" |
| T08 | R6 | Wallet modeli + vektörler | CreditManager(280) → 280 | Models/Wallet.swift (saf: coins, khTimed[expires], khPerm, fiyat tablosu, harcama sırası, 15→1, 4+1 sayacı, süre düşümü, iade aynı kova); MoonlightTests/wallet-vectors.json (horary vakası dahil); WalletTests; backend/wallet.js aynı vektörler; backend/test/wallet.test.js | XCTest + node aynı 13 vektör | A: 1/2/3 KH, horary 1 KH, ek 3 coin, 15 coin, 24 sa, 4+1 (horary sayar), reklam 1, davet 5 |
| T09a | R6 | D1 v2 + anon kimlik | worker.js(332), schema.sql(21) → 350 | schema: users(+name,+anon,+invite_code,+invited_by), wallets, kh_timed, readings, agenda, iap_transactions; /api/anon (anon_id + JWT); günlük KH hesapsıza da verilir; DeviceCheck bit0 yalnız sayaç/log (ek güvence, ret yok) | node test saf fonksiyonlar; curl | A "Keychain anonim kimlik + sunucu kaydı; DeviceCheck ek güvence" |
| T09b | R6 | Cüzdan uçları | worker.js, wallet.js, schema → 450 | /api/wallet GET, /wallet/convert, /wallet/iap, /wallet/daily-claim; /reading/* uçlarına borç/iade/4+1 atomik (horary dahil); günlük KH sunucuda sayılır | curl akışı D'ye | spec 4 "sunucu doğrusu"; "önce en erken sönecek süreli"; A "günlük KH sunucuda sayılır" |
| T10a | R6 | İnce adaptör | CreditManager(280), CreditBadge(53), TarotView/HoraryView/SettingsView/NoCreditView/WelcomeView çağrı satırları → 500 | Services/WalletManager.swift: `balance` API'si, arkada CreditManager'ı sarar; 6 view WalletManager'a bağlanır; davranış aynı | build yeşil, sim aynı | — (hazırlık) |
| T10b | R6 | Sunucu cüzdanı | WalletManager, KeychainHelper(56), ReadingAPI → 400 | Keychain anon_id, JWT, fetch/cache, optimistic düşüm; StoreKit kh1/kh3/kh10 → /wallet/iap; CreditManager çağrılmaz olur (dosya durur); Plus, credits5/10/15, hoş geldin 10, günlük 3, DEBUG 500 davranışı biter | build; sim bakiye sunucuyla eşit | A "credits ve Moonlight+ kalkıyor"; "geçiş kuralı yok" |
| T11 | R6 | Cüzdan ekranı | NoCreditView(278), CreditBadge(53), SettingsView:258-410, WelcomeView(101), NotificationManager:103-118 → 700 | Views/WalletView.swift (coin, süreli KH + kalan süre, süresiz KH, 4+1 sayacı, "15 coin → 1 KH" 15 altında pasif, "günlük KH al", davet kodu alanı (yalnız hesaplı; hesapsıza "giriş yap → davet + senkron + web"), KH paketleri); rozet coin+KH; Settings/Welcome/bildirim metinleri | sim ekran görüntüsü | A "cüzdan ekranı, günlük KH, 15 coin = süresiz KH, 4+1"; spec 4 "buton, otomatik değil" |
| T12 | R6 | Fiyat çağrı yerleri | TarotView, HoraryView, WalletManager → 950 | Tarot 1/2/3 KH, ek kart 3 coin, Horary 1 KH, ek soru 3 coin; bakiye yetmezse istek gitmez; hata → iade aynı kova; horary 4+1 sayacına girer | sim: 0 KH → cüzdan; 4. fal horary ise de +1 | A fiyatları; "Horary 4+1 sayacına girer"; spec 4 "coin ile KH karışmaz" |
| T13 | R6 | AdMob | project.yml, Info.plist, WalletView, worker.js → 500 | GoogleMobileAds SPM; 2 rewarded (kısa +1 coin günde 3, uzun → günlük süreli KH); RewardedAdManager.swift; /api/admob/ssv imza doğrulama → cüzdan; SKAdNetworkItems | sim test ad unit; SSV logu | A "reklam 1 coin", "uzun reklam → günlük KH", "kısa reklam günde 3" |
| T14 | R6 | Davet kodu | worker.js, WalletView → 400 | invite_code (yalnız hesaplı kullanıcı), /api/invite/redeem, arkadaşın ilk yorumu dönünce ikisine +5 (first_reading_done tek sefer), kendi kodu ret | node test; iki sim | A "arkadaş ilk falını bakınca ikisine +5, sunucu doğrular" |
| T15 | R7 | Mock | mock/moonlight-mock.html(268), spec 7, ContentView(90) → 450 | Aynı HTML'de 3 kalıp: katmanlı sheet (Spotify/Arc), gökyüzü haritası (Zenly/saga), yatay odalar (Instagram); 4 fal yeri + gökyüzü çizgisi | Damla seçer | A "evren hissi"; "mock, kararı Damla verir" |
| T16 | R9 | SkyEngine ay | project.yml, MoonService(150), MoonPhase(57), AstrologyService:37-82 → 350 | Packages/SkyEngine: CAstronomy (astronomy.c/.h, MIT) + Swift: illumination, phase, quarters(-14,+30), moonSign, moonIngresses; testler | XCTest bilinen dördün/dolunay ±1 sa | A "ayın durumuna göre şekli değişecek"; spec 3 "cihaz kazanır" |
| T17 | R9 | SkyEngine gezegenler | SkyEngine, HoraryChartService:44-61 → 400 | planetLongitude+speed, signIngress, retroStations, voidOfCourse(Ay), eclipses | XCTest: 2026-03-03, 2026-08-28 ay; 2026-08-12 güneş; bilinen Merkür retro | spec 3 olay listesi |
| T18a | R4 | HoraryFacts (Swift) | HoraryChartService(329), SkyEngine, ClaudeService:90-145 → 600 | Models/HoraryFacts.swift: yöneticiler, Yükselen/1. ev yöneticisi, Ay, gösterge açıları uygulayan/ayrılan + orb + kalan derece, Ay'ın sonraki açısı, boş seyir; evler yalnız API /houses; planets/aspects çağrısı kalkar | XCTest sabit chart 6 vaka | spec 2 "hazır hesaplanmış"; "chart yoksa istek atılmıyor" |
| T18b | R4 | Horary prompt Worker'a | worker.js, prompt.js, HoraryView(277), HoraryFacts → 700 | prompts/horary.txt + horary_followup.txt; /api/reading/horary: küçük sınıflandırma (kategori→ev, backend/houses.js) sonra ana yorum (Sonnet); ek soru chart'ı görür; ClaudeService kalkar | node test; 3 örnek | A "soruya göre cevap"; spec 2 kategori→ev |
| T19a | R8 | Evren kabuğu | ContentView(90), HomeView(316), Theme(180), MoonSceneView(177), seçilen mock → 900 | Seçilen kalıba göre kök view; 4 fal yeri (2 "yakında"); sahne arkada; Settings/Wallet giriş noktaları | sim | A "evren hissi"; "iskambil, Lenormand için yer" |
| T19b | R8 | Geçişler | T19a, TarotView/HoraryView üst 60 satır → 700 | jest/animasyon; üst boşluk/rozet yeni kabuğa uyar | sim video | aynı |
| T20 | R9 | Gökyüzü çizgisi | AstrologyService(189), AstroEvent(63), HomeView(316), NotificationManager:120-150 → 700 | Services/SkyEvents.swift (-14/+30: evreler saatli, ay burç değişimi, boş seyir, gezegen geçişi, retro başlangıç/bitiş, tutulma); AstrologyService, MoonService USNO ve sabit tutulma listesi kullanımdan kalkar (dosya silme yok); HomeView dikey çizgi "bugün" işaretli; DailyReadingService LLM çağrısı kalkar: "Bugünün Gökyüzü" kartı SkyEvents'ten şablon cümle üretir (ay evresi + ay burcu + aktif retro; KV/uç yok, Claude yok) | sim; XCTest olay sayısı | A "-2 hafta ve +1 ay"; "sadece gökyüzü durumları" |
| T21 | R9 | Ay çizimi + widget | MoonSceneView:167-176, HomeView:242-248, MoonlightWidget(170), NotificationManager:183-224 → 450 | Aydınlanmadan gölge maskesi (spec 3 elips) statik karakter üstüne; widget ve bildirim evre hesabı SkyEngine (project.yml iki hedef) | sim; widget = uygulama evresi | A "ayın durumuna göre şekli değişecek" |
| K4 | R10 | Faz 4 elle kurulum (Damla) | — | Ajan tarif eder: Google OAuth client id; mail servisi (Resend) hesabı + alan adı SPF/DKIM; Apple Sign in capability + Services ID; secret'lar | test mail gelir; Google token doğrulanır | A "elle kurulumlar: her fazın ilk alt görevi" |
| T22 | R10 | Apple ile giriş | worker.js:76-171, schema, WalletManager, SettingsView:1-110 → 700 | AuthManager.swift (ASAuthorizationController, fullName ilk yetkide anında POST), SignInView.swift; worker: name, anon→user birleştirme (cüzdan + readings taşınır); giriş vaadi ekranda: senkron + davet + web | sim: /api/me isim; cüzdan korunur | A "Apple ile giriş, isim zorunlu"; spec 5 "isim ilk yetkilendirmede" |
| T23a | R10 | Google | worker.js:205-216, AuthManager → 400 | GoogleSignIn SPM; JWKS yerel doğrulama (tokeninfo kalkar); e-posta UNIQUE birleştirme | sim | A "Apple, Google, e-posta+şifre" |
| T23b | R10 | E-posta + şifre | worker.js, schema, SignInView → 500 | PBKDF2-SHA256 ≥600k; verify/reset token; Resend | node test hash; sim | aynı; spec 5 |
| T24 | R11 | Senkron | ReadingStore, worker.js, schema → 600 | /api/readings (push dirty, pull since), /api/agenda; updated_at sunucu saati; son yazan kazanır (favori/silme); çevrimdışı kuyruk | iki sim | A "iki cihazda aynı hesap"; spec 5 çakışma |
| T25 | R11 | Profil + konu | SettingsView(520), BirthChartView:300-448, reading uçları → 900 | Views/ProfileView.swift: isim, burç, gündem (son 5 soru), sık konular (readings.topic: worker her yorumdan sonra 1 küçük çağrı), geçmiş, cüzdan; {{agenda}} prompt'a | sim; 3 örnek gündem cümlesi | A üyelik paragrafı; "sık konular LLM çıkarır"; "gündem API'ye gider, birikir" |
| T26a | R12 | Kart verisi: majör + yükleyici | TarotCard(193), CardDetailView:59-64,206-244 → 260 | Resources/cards.json şeması {id, name_tr, keywords_tr/en, meaning_tr/en, archetype, number, element, astro:null}; 22 majör metin; TarotCard JSON'dan yüklenir | XCTest 22 kayıt boş alan yok | A "tarot rehberi gibi"; "ters kart yok"; "şarkı/kitap yok" |
| T26b | R12 | Kart verisi: 56 minör | cards.json, TarotCard:129-188 → 300 | 56 minör özgün metin (takım×sayı, saray) | XCTest 78 kayıt | spec 6 "56 minör için özgün metin" |
| T27 | R12 | Kart sayfası + sayım | CardDetailView(245), ReadingStore, TarotView:243-288 → 600 | replace zinciri kalkar; arketip/sayı/element; "N kez geldi, son: tarih, en sık: açılım" | sim | spec 6 "kaç kez geldi" |
| T28a/b/c | R13/14/15 | Asset takası | Assets adları, damla_tasks(177), MoonSceneView, HomeView, TarotView kart hücresi → 400 | a: ay karakterleri + gökyüzü; b: 78 kart + card_back/frame; c: ikonlar + kedi + app icon 1024; .interpolation(.none) kalkar; mevcut asset dosyalarına dokunulmaz (silme yok) | build; ekran görüntüsü | damla_tasks "pixel artı bırakıyoruz" |
| T33 | R16 | Astrolojik karşılık katmanı | cards.json, prompts/tarot.txt, SpreadAnalysis, CardDetailView → 500 | cards.json astro alanı (Golden Dawn eşlemesi, kaynak yazılır); prompt'a "kart → gezegen/burç" satırı + gökyüzüyle çakışma cümlesi; rehberde bölüm | 3 örnek; XCTest 78 astro dolu | A "astrolojik karşılık katmanı: faz 6, App Store öncesi" |
| T29 | R18 | Harita evrene | BirthChartView(448), T19a kabuğu → 700 | Doğum haritası evrenden ulaşılır; BugunView kullanımdan kalkmış, silinmez | sim | A "BELKİ burç / doğum haritası" |
| T30a | R19 | Worker sertleştirme | worker.js, wrangler.toml → 500 | CORS web alanına; App Attest; IAP JWS Apple kök zinciri; kullanıcı başına limit; /api/claude ve /api/astrology/planets,aspects kapanır; secret taraması | curl 401/403; grep | CLAUDE.md §6 "secret sızmaz" |
| T30b | R19 | Metinler | privacy-policy.html, terms.html, PrivacyInfo.xcprivacy, SettingsView:411-500 → 700 | KVKK aydınlatma + ayrı açık rıza (2026/347), yurt dışı aktarım, sunucudan silme ucu + buton | Damla okur; sim silme → /api/me 404 | spec 5 KVKK |
| T31 | R20 | Hız | tüm servisler → ölçüm | açılış, /houses gecikmesi, yorum gecikmesi, prompt token; sonra en büyük 2 kalem | önce/sonra sayılar | A "ölçüm, sonra iyileştirme" |
| T32 | R21 | Submit | PROJECT.md, ASC → — | 1024 ikon, ekran görüntüleri, credits5/10/15+plus satıştan kaldırma, review notu (kombinasyon + efemeris + horary = 4.3(b) farkı) | ASC durumu | — |
Toplam: 44 alt görev (K2, K4, T01-T33; T09/T10/T18/T19/T23/T26/T30 ikiye, T28 üçe).

=====================================================================
3. GÖSTERİM SIRASI ↔ İŞ SIRASI ÇAKIŞMALARI ve KARARLAR
=====================================================================
| Çakışma | Karar | Gerekçe |
|---|---|---|
| R6 cüzdan cihazda, R10-11 sunucuda → iki kere cüzdan | Cüzdan ilk günden sunucuda: hesapsız kullanıcı /api/anon ile kimlik alır (anon_id Keychain'de), aynı JWT; R10'da anon satırı hesaba birleşir | A "Keychain anonim kimlik + sunucu kaydı; günlük KH sunucuda sayılır" |
| Tarot prompt cihazda mı sunucuda mı | Sunucuda (T05). Cihaz yapılandırılmış girdi gönderir; şablon Worker'da | Borç + yorum aynı uçta atomik; prompt ipa'dan sökülemez; web okuması aynı uç. XCTest cihazda girdi üreticisini test eder |
| Horary derinleşmesi (spec 2) B'de reels'i yok | R4'e bağlanır (T18); hakem faz 2 sonunda R4 reels metnini günceller | R3 zaten çekilmiş olacak |
| Uygulayan/ayrılan için gezegen hızı lazım; hız API'de var mı DOĞRULANMADI | SkyEngine (T16-17) R4 bloğuna, T18'den önce | API'ye yalnız /houses kalır (3 çağrı → 1) |
| Kart id'li geçmiş R12'de lazım, fallar R4'ten itibaren | ReadingStore v2 (T03) R4 bloğunun başına | R12 "kaç kez geldi" boş başlamasın |
| R7 mock Damla kararı, R8 bağlı | T15 iş sırasında öne (T01'den hemen sonra) | Karar süresi kritik yolda olmasın |
| CreditManager'ı 6 view okuyor; tek seferde değişim >300 satır | T10a ince adaptör (davranış aynı) → T10b arkasını değiştirir → T11 UI → T12 fiyat | Her adım build yeşil |
| Widget ayrı ay formülü | T21'de SkyEngine | Evre sınırında farklı ay gösterir |
| XCTest A'da "faz 2" | T01 iş sırasında T02'den sonra, faz 2 bloğunun ilk kod işi | Faz 1 kapanışı = T02 |
Önce altyapı: K2 → R6; T01 → R6; T03 → R11/R12; T05 → R4; T09a → R6/R10; T16-17 → R9 (R4 bloğunda).

=====================================================================
4. ALTYAPI KARARLARI (3'er satır)
=====================================================================
(a) Efemeris: astronomy-engine tek C dosyası (astronomy.c/.h, MIT) `Packages/SkyEngine/Sources/CAstronomy/`; SwiftPM C hedefi module map'i kendisi üretir, köprü yazılmaz. Üstüne `SkyEngine` Swift hedefi. project.yml: `packages: SkyEngine: path: Packages/SkyEngine`, Moonlight ve MoonlightWidget hedeflerinde dependency. Swift portu yok; SwiftAA (AA+ kısıtı) ve Swiss Ephemeris (AGPL) kullanılmaz.
(b) Veri: UserDefaults → Application Support altında Codable JSON dosyaları (readings.json, agenda.json, wallet-cache.json) tek `ReadingStore` actor'ü arkasında; SwiftData/GRDB yok (bağımlılık/makro maliyeti, 8 GB makine, birkaç bin kayıt). D1: users, wallets, kh_timed, readings(id, user_id, type, spread, question, cards json, reading, sky json, topic, created_at, updated_at, deleted), agenda, iap_transactions, invites. Senkron: okuma satırı append-only; favori/silme "son yazan kazanır", zaman damgası SUNUCU saati; cüzdan senkron değil, sunucu tek doğru, cihaz önbellek + optimistic düşüm.
(c) XCTest: project.yml'e `MoonlightTests: type: bundle.unit-test, sources: MoonlightTests, dependencies: [target: Moonlight]` + `schemes: Moonlight: test: targets: [MoonlightTests]`; `xcodebuild test -scheme Moonlight -destination 'platform=iOS Simulator,name=iPhone 16'`. İlk testler: 78 kart, evre eşikleri (T01); SpreadAnalysis 8 vaka (T04); Wallet 13 vektör (T08); SkyEngine bilinen tarihler (T16-17); HoraryFacts (T18a). Worker: `node --test backend/test/`, aynı wallet-vectors.json.
(d) Prompt'lar: Worker'da `backend/prompts/*.txt` şablon ({{alan}}), wrangler `rules = [{type="Text", globs=["prompts/*.txt"]}]`; `backend/prompt.js` doldurur. Hakem ve Damla prompt'u kodsuz okur; cihazda prompt metni yok. Model Sonnet (A); faz 2'de aynı 3 açılımla Haiku vs Sonnet Damla'ya (T05 kanıtı). Fable yok.
(e) Kullanımdan kalkanlar (kod yolu kapanır, dosya durur): CreditManager → WalletManager (T10a/b; Plus, credits, hoş geldin, günlük 3, DEBUG 500 davranışı biter — CreditManager.swift T10b'den sonra hiçbir yerden çağrılmaz, dosya durur); ilişki/kariyer açılımı (T06); ClaudeService (T18b sonrası çağrılmaz); Element "%" prompt'tan çıkar (T05); AstrologyService/MoonService USNO/sabit tutulma (T20'de çağrılmaz); DailyReadingService LLM çağrısı (T20'de şablon cümle). Kod dosyaları silinmez, asset dosyalarına dokunulmaz (§7). MoonlightWidget kalır, SkyEngine'e bağlanır.
(f) Hesapsız + reinstall (A + 9 Eyl): uygulama ilk açılışta /api/anon → sunucu anon kullanıcı + cüzdan yaratır, anon_id Keychain'de (ThisDeviceOnly; silme-yükleme sonrası kalır, garanti değil). Günlük KH hesapsıza da verilir ve 4+1 ile birlikte sunucuda sayılır; reinstall aynı anon_id ile aynı cüzdana döner, gün içinde ikinci KH yok. Giriş yapmanın getirdiği: bulut senkron, davet kodu, web okuması — para için zorunlu değil.
    daily_blocked kuralı GEREKMEZ: reinstall koruması Keychain anon_id ile sunucuda çözülüyor; Keychain'in de silindiği tek durum (cihaz sıfırlama) günde 1 süreli KH kazandırır, kötüye kullanım maliyeti cihaz sıfırlamak. DeviceCheck ek güvence olarak yalnız ölçer: /api/anon DCDevice token'ıyla gelir, bit0=1 iken yeni anon açılıyorsa sayılır/loglanır (T09a), ret yok; sayı büyürse kural T30a'da yeniden konuşulur. last_update_time ay çözünürlüğü DOĞRULANMADI, T09a ajanı Apple dokümanından doğrular.

=====================================================================
5. COMPOUNDING ERROR — her R sonunda hakemin yeniden koşacağı önceki kanıtlar
=====================================================================
| Biten | Yeniden koşar |
|---|---|
| R1 | build; isimsiz → kapı; 3 tarot + 1 horary örneği isimli |
| R4 | R1 örnekleri yeniden: isim var, "dağılım" cümlesi ilk paragrafta, kart kart paragraf yok, 3/5 kartta "Geçmiş/Şimdi/Gelecek" etiketi yok; R2: prompt'ta ay evresi + ay burcu + iç gezegen retro, "Plüton retro" yok, Element % yok; model Sonnet (worker log); `xcodebuild test` + `node --test` |
| R5 | R4 kontrolü 3/5/Kelt için; ilişki/kariyer ekranda yok; ek kart yorumu önceki kartlara atıf yapıyor; Kelt yorumu kesilmiyor |
| R6 | R4-R5 örnekleri borç sonrası; wallet vektörleri Swift+JS; 14 coin'de buton pasif; 4. fal (horary dahil) kh_perm+1; iade eski expires_at; DEBUG'da 500 kredi yok; "Moonlight+" grep boş |
| R8 | R1-R6 ekranları yeni kabuktan; rozet/üst boşluk kaymamış; cüzdan erişilir |
| R9 | olay listesi yerine çizgi; bilinen tarih testleri; widget = uygulama evresi; R4 prompt'ta ay burcu SkyEngine'den ve API ile aynı (1 gün örnek) |
| R10 | R6 bakiye girişten sonra aynı; anon satırı kapandı; isim /api/me'de; R1 kapısı hesaplıda da |
| R11 | R10; iki cihaz geçmişi eşit; R4 prompt'ta {{agenda}} dolu; 4+1 sayacı iki cihazda aynı |
| R12 | R11 geçmiş kart id'leri sayımla eşit; kart adları TR; 78 kayıt boş alan yok |
| R13-15 | R8-R9 ekranları yeni assetlerle; Image(ad) kırık yok |
| R16 | R4-R5 örnekleri astro satırıyla yeniden: kombinasyon cümlesi kaybolmamış, isim var; rehberde 78 astro alanı dolu |
| R19 | curl 401/403; grep secret; R6 SSV/IAP yolu geçiyor; privacy metni ekranla tutarlı |
| R20 | R4-R5 örnekleri aynı kalitede |
Sabit: `xcodebuild build` + `xcodebuild test` + `node --test` yeşil; "yapma" listesi (Plus, paylaşım kartı, pixel font, ilişki/kariyer, ters kart, seçmeli gündem, şarkı/kitap, 3/5'te sabit pozisyon) grep ile.

=====================================================================
6. GERÇEK İŞ SIRASI (tek liste)
=====================================================================
1 T02 (R1) ── Faz 1 kapanış ──
2 K2 kurulum (R6) → 3 T01 (R6) → 4 T15 mock (R7, Damla karar süresi başlar) → 5 T03 (R11/R12) → 6 T04 (R4) → 7 T05 (R4) → 8 T06 (R5) → 9 T07 (R5) →
10 T08 (R6) → 11 T09a (R6) → 12 T09b (R6) → 13 T10a (R6) → 14 T10b (R6) → 15 T11 (R6) → 16 T12 (R6) → 17 T13 (R6) → 18 T14 (R6) →
19 T16 (R9) → 20 T17 (R9) → 21 T18a (R4) → 22 T18b (R4) ── Faz 2 kapanış (Haiku/Sonnet kararı dahil) ──
23 T19a (R8, mock kararı) → 24 T19b (R8) → 25 T20 (R9) → 26 T21 (R9) ── Faz 3 ──
27 K4 kurulum (R10) → 28 T22 (R10) → 29 T24 (R11) → 30 T25 (R11) → 31 T23a (R10) → 32 T23b (R10) → 33 T26a (R12) → 34 T26b (R12) → 35 T27 (R12) ── Faz 4 ──
36-38 T28a/b/c (R13-15, Damla çizince; paralel) ── Faz 5 ──
39 T33 (R16) → R17 hakem → 40 T29 (R18) ── Faz 6 ──
41 T30a (R19) → 42 T30b (R19) → 43 T31 (R20) → 44 T32 (R21) ── Faz 7 ──

=====================================================================
7. KULLANIMDAN KALKAN KOD (silinmez; asset dosyalarına dokunulmaz)
=====================================================================
| Kalem | Bugün | Ne olur |
|---|---|---|
| Element.swift (63) | Uydurma "%" değerleri prompt'a giriyor | T05'te prompt'tan çıkar; dosya durur |
| BugunView.swift (213) | Referanssız, derleniyor | Dokunulmaz; R18'de ekran olarak kullanılabilir |
| MoonService USNO (:14-60) + uydurma doğuş/batış (:99-106) | Home doğuş/batış buradan | T20'de SkyEngine verir, USNO çağrılmaz; kod durur |
| AstrologyService sabit tutulma listesi (:136-188), +14 gün penceresi | 2027'de biter | T20'de SkyEngine hesaplar, liste çağrılmaz; kod durur |
| DailyReadingService (48) | Kullanıcı başı günde 1 Claude çağrısı, açık /api/claude üzerinden | T20'de LLM çağrısı kalkar; kart SkyEvents'ten şablon cümle üretir; sınıf adı kalır |
| CreditManager.swift (280) | Plus/credits | T10b'de kullanımdan kalkar (A "kalkıyor"); dosya silinmez, hiçbir yerden çağrılmaz |
| ClaudeService.swift (280) | 5 prompt cihazda | T05/T07/T18b sonrası çağrılmaz; dosya durur |
| Asset katalogu (blink_*, glow_*, bg_full_moon_v*, sparkle vb.) | Kullanılmayan setler var | DOKUNULMAZ (karar) |

=====================================================================
SORULMAMIŞ AMA GÖRDÜKLERİM
=====================================================================
- 0809-kosu.md B R12 satırında "şarkı/kitap" hâlâ yazıyor; A 9 Eyl "bu turda yok" diyor (Damla düzeltir). Spec 8:179 "4 tarot açılımı" A'nın "horary sayar" kararıyla çelişiyor (spec düzeltilmeli).
- /api/claude app-token'la açık proxy; token ipa'dan çıkarılabilir. T05/T18b ile yorumlar taşınır, T20'de DailyReadingService çağrısı kalkar, T30a'da uç kapanır.
- privacy-policy.html R6'dan itibaren yanlış (hesap/DB/reklam var); T30b R19'da. R19 öncesi TestFlight dağıtılmaz.
- BirthChartView + NatalChartService gerçek doğum haritası hesaplıyor; R18 için zemin hazır.
- Repo kökünde Next.js sitesi (app/, next 16.1.6) ve mock/moonlight-mock.html var; web okuması için altyapı, plan dışı.
- HoraryChartService "topocentric"; SkyEngine geocentric → T18'den sonra fark kalkar. FreeAstrologyAPI /planets hız alanı DOĞRULANMADI.
- EngagementPrompts.swift (puan isteği, bildirim izni) A'da yok, dokunulmadı. NotificationManager:103-118 "krediler yenilendi 11:00" T11'de "günlük KH hazır" olur.
- GoogleMobileAds SPM büyük; 8 GB makinede ilk derleme uzar; T13 tek oturum.
- Kullanılmayan asset setleri (blink_* 32, glow_* 32, bg_full_moon_v2/3/5/6/7) ipa boyunu şişiriyor; karar "dokunulmaz", boyut R20 ölçümünde görünür.
- Görmediklerim: simülatörü çalıştırmadım (plan turu); FreeAstrologyAPI ham JSON; Apple DeviceCheck dokümanı (T09a doğrular).
